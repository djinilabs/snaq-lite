# Active context

## Just completed
- **Review and improve (one-output-to-two-inputs):** (1) **DRY:** Extracted `run_node_with_graph_inputs_impl(order, sources, unit_registry, incoming_map, sink_uri)` from the native and WASM `run_node_with_graph_inputs`; both now acquire (order, sources, unit_registry, incoming_map) then call the shared sync impl, removing ~80 lines of duplication. (2) **Docs:** [EXTERNAL_STREAMS.md](docs/EXTERNAL_STREAMS.md) — added bullet "Graph fan-out" under Limits: when one computation output is wired to multiple inputs, the runtime creates one handle per edge and feeds a copy to each. (3) **Optimization:** Considered avoiding one clone in feed_value_to_senders for the first vector sender; reverted (would need to move collected into first sender then clone for rest; we need collected for all). pnpm test, pnpm run lint pass.
- **One output to two inputs (stream input not available):** When one computation output was wired to two different inputs of another box, the LSP reused a single stream handle for both; the first InputDecl consumed it and the second got StreamInputNotAvailable. (1) **Fix:** [crates/snaq-lite-lsp/src/lib.rs](crates/snaq-lite-lsp/src/lib.rs) — `run_node_with_graph_inputs` (native and WASM) now stores `output_values: HashMap<Url, (Value, Database)>` instead of one handle per source; when building stream_inputs we create one handle per edge (per target input name), group senders by source_uri, and call `feed_value_to_senders(value, db, senders)` so each input gets its own copy of the upstream value (scalar: one chunk each; vector: collect once via `collect_vector_stream` then send to all). (2) **Lang:** [queries.rs](crates/snaq-lite-lang/src/queries.rs) `collect_vector_stream` made pub and re-exported from lib.rs for LSP fan-out. (3) **Test:** [lsp_integration.rs](crates/snaq-lite-lsp/tests/lsp_integration.rs) `native_one_output_to_two_inputs_same_box_yields_result` — one output (42) wired to inputs abc and ggg, body "abc * ggg", assert Completed display "1764". Removed unused `feed_vector_to_chunk_sender`. pnpm test, pnpm run lint pass.
- **Undo-restored edge has no effect (LSP not synced):** After remove edge then undo, the store restored the edge but the LSP was never told to re-add it, so the computation result stayed symbolic (e.g. "10*abc") instead of evaluated (e.g. "40"). (1) **Root cause:** Undo/redo only restore Zustand state; they did not call syncLoadedGraphToLsp. (2) **Fix:** New [perform-undo-redo.ts](apps/frontend/src/lib/perform-undo-redo.ts) with performUndo/performRedo (store undo/redo + syncModelsToGraphNodes + syncLoadedGraphToLsp); [project.$projectId.tsx](apps/frontend/src/routes/project.$projectId.tsx) and [project-toolbar.tsx](apps/frontend/src/components/project-toolbar.tsx) use them for keyboard and toolbar. (3) **Test:** [perform-undo-redo.test.ts](apps/frontend/src/lib/perform-undo-redo.test.ts) — after removeEdge and performUndo, syncLoadedGraphToLsp is called with restored edges; performRedo test too. pnpm test, pnpm run lint, pnpm build (frontend) pass.
- **Input rename unbound (LSP graph by name):** When the user renamed an input (e.g. "x" → "abc") and updated the code, the wired value was not bound because the LSP graph stores edges by **targetInputName**; the frontend only sent graph/connect at wire time. (1) **Fix:** [edge-handlers.ts](apps/frontend/src/components/canvas/edge-handlers.ts) — added `syncIncomingEdgesToLsp(targetId)`: sends didOpen for the target with current document content, then graph/connect for each incoming edge with the **current** input name at that index, then increments the target's widget version. (2) [computation-box-node.tsx](apps/frontend/src/components/canvas/computation-box-node.tsx) — after `setNodeInputs` (in updateInput, addInput, removeInput) calls `void syncIncomingEdgesToLsp(id)` so the LSP binding updates when the user renames an input. (3) Unit tests in edge-handlers.test.ts: syncIncomingEdgesToLsp sends didOpen + graph/connect with current names; after renaming input, sends graph/connect with new name. (4) E2E test "after renaming input and updating code, wired computation still shows bound result" added but **skipped** (LSP/widget timing in E2E; same as 420 test). pnpm test (258 frontend), pnpm run lint pass.
- **Remove connections between blocks in the UI:** (1) **Graph canvas:** Enabled edge selection and deletion via `defaultEdgeOptions={{ selectable: true, deletable: true }}` on ReactFlow so users can click an edge and press Delete/Backspace to remove it; `onEdgesDelete` already called `disconnectEdge(targetUri, targetInputIndex)` from [edge-delete-params](apps/frontend/src/components/canvas/edge-delete-params.ts). (2) **Types:** Fixed FlowNodeData → Node `data` cast in graph-canvas (as unknown as Record<string, unknown>) for React Flow typings. (3) **E2E:** New test "removing connection by selecting edge and pressing Delete restores presentation placeholder" in [canvas.spec.ts](apps/frontend/e2e/canvas.spec.ts) — wire computation (7) to presentation, click edge midpoint, focus graph wrapper, press Delete, assert "Connect a computation box" visible again. pnpm test, pnpm run lint pass; E2E passes with PLAYWRIGHT_REUSE_SERVER=1.
- **Review and improve (connection-by-index tests):** (1) **sync-graph-to-lsp:** Skip sending graph/connect when target input at index has empty or whitespace name (align with connectEdge); added test that skips such edges; first test now asserts didOpen (sendNotification) called for each node. (2) **edge-handlers:** New test "sends current target input name at index to LSP (rename-before-disconnect uses new name)" — disconnect after renaming input asserts LSP receives new name. (3) **edge-delete-params:** New test "omits edges when targetHandle is negative or non-integer" (-1, 1.5 omitted). All 252 frontend tests and lint pass.
- **Connection by input index (not name):** Edges now store **targetInputIndex** (0-based) so renaming an input does not remove the connection. Graph store, project types, and persistence use targetInputIndex; LSP still receives **targetInputName** (derived from node.inputs[index].name) for graph/connect and graph/disconnect. Handles use id = index (e.g. "0", "1"); computation and presentation input handles use data-testid by index (computation-input-handle-0). Backward compat: parseProjectSnapshot accepts legacy targetInputName; snapshotEdgesToGraphEdges resolves name → index on load. [graph-store.ts](apps/frontend/src/store/graph-store.ts), [project.ts](apps/frontend/src/types/project.ts), [project-loader-utils.ts](apps/frontend/src/lib/project-loader-utils.ts), [edge-handlers.ts](apps/frontend/src/components/canvas/edge-handlers.ts), [graph-canvas.tsx](apps/frontend/src/components/canvas/graph-canvas.tsx), [edge-delete-params.ts](apps/frontend/src/components/canvas/edge-delete-params.ts), [sync-graph-to-lsp.ts](apps/frontend/src/lib/sync-graph-to-lsp.ts), [computation-box-node.tsx](apps/frontend/src/components/canvas/computation-box-node.tsx), [presentation-block-node.tsx](apps/frontend/src/components/canvas/presentation-block-node.tsx). Unit tests and E2E updated (computation-input-handle-0 for first input). pnpm test, pnpm run lint, pnpm build pass; smoketest "wiring computation output to another computation input" may timeout in CI (flaky).
- **Named argument still not working in UI (LSP got body-only):** (1) **Symptom:** Wiring A ("4222") to input "fff" in B ("fff * 3333562122111") still showed "3333562122111*fff" and presentation "variable binding: symbolic value not yet supported". (2) **Cause:** Backend fix (Block binding from stream) only runs when the parsed program has InputDecl; the **frontend** was sending didOpen/didChange with **body only** (editor model value), so the LSP document never contained "input fff: Numeric" and the program was a single expression, not a block. (3) **Fix (frontend):** Always send **full document** (input decls + body) for computation nodes: new [lib/computation-document-content.ts](apps/frontend/src/lib/computation-document-content.ts) `buildComputationDocumentContent(body, inputs)`; use it in ComputationBoxEditor (didOpen + didChange), ComputationBoxNode (onBeforeSubscribe), edge-handlers (computationDocumentContent), and sync-graph-to-lsp (doSyncWithClient for computation nodes). So the LSP always has e.g. "input fff: Numeric\nfff * 3333562122111" and the backend can bind fff from the stream. (4) **Tests:** computation-document-content.test.ts; pnpm test, pnpm run lint pass.
- **Named argument not included when wiring computation to computation:** (1) **Bug:** Wiring box A ("42") to input "abc" in box B ("input abc: Numeric\nabc * 10") yielded symbolic "10 * abc" instead of 420. (2) **Cause:** InputDecl is metadata-only; stream inputs are only consumed via `$name` (ExternalStream), so identifier `abc` was never bound. (3) **Fix (lang):** In Block evaluation ([queries.rs](crates/snaq-lite-lang/src/queries.rs)), for each InputDecl(name, _) that has a stream input handle, call new `stream_input_to_value(handle_id)` (take_receiver, ChunkFlattenStream, block_on collect; one element → that Value, multiple → Vector), then extend scope with name → StoredValue::from_value(value). (4) **Tests:** Lang unit test `run_input_decl_binds_stream_input_to_identifier` (program "input abc: Numeric\nabc * 10" with stream_inputs {"abc": handle} sending 42 → assert result 420); LSP integration test `native_computation_to_computation_wired_input_binds_identifier` (upstream "42", downstream "input abc: Numeric\nabc * 10", connect, subscribe → assert Completed display "420"). pnpm test, pnpm run lint pass.
- **Computation-to-computation wiring ("target has no input named 'x'"):** (1) **Root cause:** LSP only knows inputs from the **parsed document**. If the user adds an input via the UI but never types "input x: Type" in the editor, the LSP document has no input decls → connect fails. (2) **Fix:** In connectEdge, for computation targets send didOpen with content = **UI inputs + body**: computationDocumentContent(targetUri, targetNode) builds input decls from targetNode.inputs and prepends to body (from getModel or initialContent) when body doesn't already start with "input ". So LSP always has the declaration. (3) **Widget refresh:** After addEdge on connect success, increment target's widget content version (useWidgetContentVersionStore.increment('computation-result-' + targetId)) so the target re-subscribes and receives the new result (420). (4) **Fallback:** If content would be empty but store has the target input (targetInputName), send minimal "input name: type" so connect still succeeds. (5) **edge-handlers.ts:** getModel, computationDocumentContent, didOpen for computation (when content non-empty), then graph/connect, then increment. E2E wiring test passes.
- **E2E test: computation block input wiring (42 → x; result 420):** (1) [computation-box-node.tsx](apps/frontend/src/components/canvas/computation-box-node.tsx) — added `data-testid={\`computation-input-handle-${inp.name}\`}` on input Handles so e2e can target a specific port. (2) [canvas.spec.ts](apps/frontend/e2e/canvas.spec.ts) — new test "wiring computation output to another computation input shows combined result (420)": two computation blocks; first has input x (Numeric), body "input x: Numeric" + newline + "$x * 10"; second has body "42"; wire second output → first input x; assert first block result shows 420 (timeout 40s). Test may time out until computation-to-computation wiring is fixed in LSP/frontend. pnpm test:frontend, pnpm run lint:frontend pass.
- **Input argument type select closing immediately:** (1) **Cause:** Click on the type `<select>` bubbled to React Flow's pane, so `onPaneClick` ran (clear selection) and the native dropdown closed. (2) **Fix:** [computation-box-node.tsx](apps/frontend/src/components/canvas/computation-box-node.tsx) — added `onMouseDown` and `onClick` with `stopPropagation()` on the inputs container (NodeContentZone wrapping the inputs list and "+ Add input" button) so pane does not receive the event. (3) **E2E:** New test "Input argument type select stays open when clicked" — add computation, add input, set `__E2E_DEBUG_CLICKS__`, click type select, assert `__E2E_LAST_CLICK__` is not `'pane'`, then selectOption('Numeric') and assert value. (4) **Instrumentation:** [graph-canvas.tsx](apps/frontend/src/components/canvas/graph-canvas.tsx) — when `window.__E2E_DEBUG_CLICKS__` is true, `onNodeClick`/`onPaneClick` set `window.__E2E_LAST_CLICK__` to 'node'/'pane' so E2E can assert pane did not receive the click. pnpm test, pnpm run lint, pnpm build pass; canvas E2E test passes with PLAYWRIGHT_REUSE_SERVER=1.
- **Review and improve (presentation + sync):** (1) **DRY PresentationBlockNode:** Extracted `sendDidOpenForPresentation(uri, inputs)` and use it in both the mount useEffect and in `onBeforeSubscribe` so didOpen logic lives in one place. (2) **Single pending sync:** [sync-graph-to-lsp.ts](apps/frontend/src/lib/sync-graph-to-lsp.ts) keeps a module-level `pendingSync`; when `waitForLanguageClient` times out we set `pendingSync = { nodes, edges }` and register `whenClientReady`. The callback runs `const p = pendingSync; pendingSync = null; if (p) doSyncWithClient(...)` so only the **latest** load is synced when the client becomes ready (avoids syncing stale project if user navigated before client ready). (3) **Types:** `doSyncWithClient` now takes `LanguageClientLike` from the singleton. (4) **Test:** presentation-block.test.tsx — "calls onBeforeSubscribe before subscribeWidget when provided (avoids source document not open)" with fake timers to assert onBeforeSubscribe runs and subscribeWidget is sent after delay. pnpm test (frontend), pnpm run lint pass.
- **Unbound stream input after refresh (sync when LSP ready):** If the LSP worker is not ready within the sync timeout (e.g. slow WASM load after full page refresh), sync gave up and never sent graph/connect, so the presentation kept showing "unbound stream input". (1) **Fix:** [sync-graph-to-lsp.ts](apps/frontend/src/lib/sync-graph-to-lsp.ts) — when `waitForLanguageClient(LSP_LOAD_SYNC_WAIT_MS)` returns null, register via `whenClientReady()` to run the same sync (didOpen + graph/connect) when the client is set; [language-client-singleton.ts](apps/frontend/src/lsp/language-client-singleton.ts) — added `whenClientReady(cb)` and invoke callbacks in `setLanguageClient` when client becomes available. (2) Sync timeout increased to 25s (matches E2E assertion). (3) Unit tests: `whenClientReady` runs immediately if client set, or when `setLanguageClient` is called later. E2E "after full page refresh" remains the test that asserts presentation shows "7" and not unbound.
- **Unbound stream input in presentation after refresh:** (1) **Cause:** On load, setGraph runs synchronously then syncLoadedGraphToLsp runs in background; presentation block subscribes before graph/connect is sent, so LSP has no edges and returns Error (unbound $x). When sync later sends graph/connect, the LSP did not refresh the widget. (2) **Fix:** [crates/snaq-lite-lsp/src/lib.rs](crates/snaq-lite-lsp/src/lib.rs) — extracted `refresh_widgets_for_uri(uri)` from refresh_downstream_widgets; after `graph_connect` adds the edge, call `refresh_widgets_for_uri(target_uri)` and `drain_widget_notifications()` so any already-subscribed presentation widget gets re-run with the new graph and receives Completed. (3) **LSP test:** `native_subscribe_then_connect_refreshes_widget_with_result` — open comp "7" and pres "input x: Undefined\n$x", subscribeWidget (get Error), then graph/connect, assert connect succeeds. (4) **E2E:** "after full page refresh" test now also asserts presentation shows "7" and does not show "unbound stream input". pnpm test, pnpm run lint, pnpm smoketest (37 E2E) pass.
- **Connected blocks disappear after refresh:** (1) **Cause:** When snapshot had edges, use-project-loader only called setGraph inside the .then() of syncLoadedGraphToLsp; if that promise rejected or the effect cleaned up before it resolved, setGraph never ran and the graph stayed empty. (2) **Fix:** [use-project-loader.ts](apps/frontend/src/hooks/use-project-loader.ts) — setGraph(nodes, edges) and clearAll() are now called synchronously whenever a snapshot exists; if edges.length > 0 we run syncLoadedGraphToLsp in the background (void + .catch()). Blocks appear immediately; presentation may briefly show "unbound stream input" until sync completes. (3) **E2E:** New test "after full page refresh, connected computation and presentation blocks both reappear" in canvas.spec.ts — add comp + presentation, wire them, page.reload(), assert both nodes visible (timeout 25s). (4) **Unit test:** [use-project-loader.test.ts](apps/frontend/src/hooks/use-project-loader.test.ts) — sync load path: getProjectSnapshot → snapshotToGraphNodes → setGraph; asserts store has 2 nodes and 1 edge so graph is visible even when sync would reject. pnpm test, pnpm run lint, pnpm build pass; E2E "full page refresh" test passes.
- **wasm-opt reactivated for LSP WASM build:** (1) Removed `wasm-opt = false` from `crates/snaq-lite-lsp/Cargo.toml` so wasm-pack runs wasm-opt on release build. (2) CI: added step "Install binaryen (wasm-opt for LSP WASM build)" before smoketest so `build:lsp-wasm` has wasm-opt available on Ubuntu. (3) docs/LSP.md: note that release build uses wasm-opt; install binaryen (macOS: brew, Ubuntu: apt) if needed.
- **Presentation shows scalar as number not vector:** (1) **Cause:** When a computation outputs a scalar (e.g. 7) and is wired to a presentation, the LSP feeds it as a single-element stream so the presentation result is `Value::Vector` (one element). We were sending Completed with `totalElements: 1` (native stream consumer) or `display: "[7]"` (WASM `format_value_for_display`), so the UI showed "1 elements" or "[7]". (2) **E2E test:** `wired presentation shows scalar result as number not as vector` — wire comp "7" to presentation, assert content shows "7" and does not show "elements" or "[7]". (3) **Native:** `run_stream_consumer_for_widget` now tracks the first element's `display`; when `total == 1` the Completed payload includes `display` so the frontend shows the number. (4) **WASM/refresh:** Added `format_vector_for_widget_display` in snaq-lite-lang: for a single-element vector returns that element's display string, else bracket form. LSP subscribe_widget and refresh_downstream_widgets use it for Vector results on WASM. (5) **Lang:** Extracted `format_vector_for_display` for the Vector branch of `format_value_for_display`. Lint fix: removed unused `getLanguageClient` from sync-graph-to-lsp.ts.
- **Presentation not updating fix (frontend):** The presentation block was subscribing with the **computation's** URI (source of the edge), so the LSP registered the widget under that URI. When the computation changed, `refresh_downstream_widgets(computation_uri)` gets targets = [presentation_uri] and calls `take_entries_for_uri(presentation_uri)` — which looks for widgets whose registered URI is the **presentation** document. So no widget was found. **Fix:** Subscribe with the **presentation** document URI (`documentUri`). Added `documentUri` prop to `PresentationBlock`; `PresentationBlockNode` passes `documentUri={data.uri}`. When wired, `useSubscribeWidget` now receives `sourceUri: documentUri` (the presentation URI). The LSP then registers the widget under the presentation URI, so refresh finds it and pushes the update. Tests updated; 230 frontend tests pass.
- **LSP test: downstream widget receives push update when source changes:** New integration test `native_downstream_widget_receives_push_update_when_source_changes` (no longer ignored). Ensures that when a node is subscribed to another node’s result (wired graph: source "7", target "input x: Undefined\n$x"), (1) after subscribe + hover (to drain), client gets first `widgetDataUpdate` Completed with display "7" or totalElements 1; (2) after `didChange` source to "100" and a second hover (to drain on native), client gets a second Completed with display "100" or totalElements 1, and no Cancelled. On native, vector results are sent by a background thread, so the test sends a hover after subscribe and after didChange to trigger `drain_widget_notifications` and receive the notifications.
- **Presentation updates when computation changes (push, no re-subscribe):** (1) **LSP:** Scalar widget subscriptions are now registered via `insert_scalar` so downstream widgets can be refreshed. On `did_change`/`did_open` we call `refresh_downstream_widgets(source_uri)`: take widget entries for each target URI, cancel any stream consumer, re-run `run_node_with_graph_inputs(target_uri)`, send new `widgetDataUpdate` Completed (or Error) and re-insert scalar so next change pushes again. No `Cancelled` is sent for downstream widgets. (2) **Widget registry:** `take_entries_for_uri` (returns widget_id + Option&lt;cancel_tx&gt;), `insert_scalar(widget_id, source_uri)`; `remove` returns `Option<Option<Sender>>` so unsubscribe can send Cancelled for scalar widgets. (3) **Frontend:** Removed re-subscribe-on-Cancelled logic from PresentationBlock (no subscribeKey/useEffect); single subscribe and LSP pushes updates. (4) **E2E:** `__E2E_WIDGET_LOG__` in widget store logs each setWidget (widgetId, status, display); canvas spec sets it via addInitScript and on assertion failure attaches widget log. (5) **WASM transport:** `WasmMessageWriter::poll_write` calls `poll_flush` immediately so notifications are not buffered. Run E2E with port 3000 free so smoketest uses fresh build.
- **Fix "target has no input named 'input'" when wiring to presentation:** (1) **Root cause:** Default presentation document used input name "input", but "input" is a language keyword so the lexer tokenizes it as Tok::Input; the parser never sees an Ident for the name, so the declaration does not parse and extract_input_decls_from_block finds no input. (2) **Fix:** Use non-keyword name "x": DEFAULT_PRESENTATION_DOCUMENT_CONTENT = 'input x: Vector\n$x'; presentation Handle id="x" (was id="input"). (3) **E2E:** Assert "target has no input named 'input'" does not appear in wiring test.
- **Fix "target document not open" when wiring computation → presentation:** (1) **Root cause:** LSP graph_connect requires both source and target documents open; presentation nodes never sent didOpen. (2) **Constants:** DEFAULT_PRESENTATION_DOCUMENT_CONTENT in lib/constants.ts (`input x: Vector\n$x`; name must be non-keyword, matches Handle id). (3) **PresentationBlockNode:** useEffect sends didOpen for node URI when LSP ready, content from node.inputs or default; data-testid="presentation-input-handle" on Handle. (4) **connectEdge:** Before graph/connect, if target is presentation node, send didOpen for targetUri so LSP has document at connect time. (5) **Handles:** data-testid="computation-output-handle" on computation source Handle. (6) **E2E:** canvas.spec.ts test "wiring computation output to presentation input does not show 'target document not open'" — add comp + presentation, wait for editor visible, drag output handle to presentation input handle, assert no error toast. (7) **Tests:** constants.test.ts asserts DEFAULT_PRESENTATION_DOCUMENT_CONTENT. pnpm test (230 frontend), pnpm run lint pass; canvas E2E 21 passed (with PLAYWRIGHT_REUSE_SERVER=1).
- **Editor focus loss when repeatedly typing same digit:** (1) **Root cause (E2E log):** LSP sends `nodeSignatureUpdated` on content change; `applyNodeSignature` always did `set({ nodes: state.nodes.map(...) })`, so the graph store got a new `nodes` reference every time. That re-rendered project page, graph canvas, and every computation node → editor re-renders and focus moved to body. (2) **Fix:** [graph-store.ts](apps/frontend/src/store/graph-store.ts) — `applyNodeSignature` now returns `state` unchanged when the node's `outputType` is already equal to the new value, so repeated LSP updates with same type do not trigger re-renders. (3) **Focus restore:** [computation-box-editor.tsx](apps/frontend/src/components/editor/computation-box-editor.tsx) — added `requestAnimationFrame` after `useLayoutEffect` focus restore so we catch focus lost to async DOM updates. (4) **E2E test:** "focus: repeatedly typing same digit reproduces focus loss and logs correlation" — types "4" 50 times with 80ms delay, checks focus after each keystroke, on failure includes last 60 events in assertion message; test passes consistently. (5) **Unit test:** graph-store "applyNodeSignature does not update state when outputType is unchanged". pnpm test, pnpm run lint pass; E2E focus test run 3× passed.
- **Editor focus loss when result updates:** (1) **Test:** [computation-box-editor.test.tsx](apps/frontend/src/components/editor/computation-box-editor.test.tsx) asserts the editor effect dependency array does not include `initialContent` (source-inspection test to avoid loading Monaco in tests). (2) **Fix:** [computation-box-editor.tsx](apps/frontend/src/components/editor/computation-box-editor.tsx) — removed `initialContent` from the `useEffect` dependency array; added eslint-disable with comment that including it would recreate the editor on parent re-renders (e.g. when widget/result updates) and cause focus loss. `initialContent` is still used inside the effect for `getOrCreateModel(uri, monaco, initialContent)` (only when creating a new model). pnpm test (frontend 225), pnpm run lint (frontend) pass.
- **E2E editor–worker protocol test:** (1) New [apps/frontend/e2e/computation-result.spec.ts](apps/frontend/e2e/computation-result.spec.ts): test "computation block shows result after typing expression" (gotoCanvas, add computation, click editor zone, keyboard.type('42'), expect result "42", timeout 20s); test "protocol: didOpen, subscribeWidget, widgetDataUpdate over worker" (addInitScript for __E2E_LSP_LOG__, clear log after editor visible, type "42", assert log has didOpen out, subscribeWidget out, widgetDataUpdate in with status Completed). Both tests are **skipped** until the app shows the computation result (known LSP/worker flow bug). (2) [message-router.ts](apps/frontend/src/lsp/message-router.ts): when window.__E2E_LSP_LOG__ exists, push out/in LSP message descriptors (method; for notifications, params) so E2E can assert protocol. pnpm smoketest: 26 passed, 2 skipped.
- **Monaco worker configuration:** Added `src/monaco-environment.ts` that sets `self.MonacoEnvironment.getWorker` using Vite's `?worker` import for `monaco-editor/esm/vs/editor/editor.worker.js`, so Monaco creates web workers instead of falling back to the main thread. Imported at top of `entry-client.tsx` so it runs before any Monaco code. Fixes console warnings "Could not create web worker(s)" and "You must define MonacoEnvironment.getWorkerUrl or MonacoEnvironment.getWorker". pnpm test, pnpm run lint, pnpm build (frontend) pass.
- **Review and improve (auto-save / undo/redo tests):** graph-store.test.ts: added tests that when getter is set, removeNode pushes to undo (undo restores node); addEdge pushes to undo (undo removes edge); setNodeInputs pushes to undo (undo restores previous inputs); when getter throws, mutator does not push; setGraph without clearHistory preserves undo and redo stacks. 216 frontend tests, pnpm test and pnpm run lint pass.
- **Auto-save, remove Save button, Undo/Redo (last 10 states):** (1) **Constants:** AUTO_SAVE_DEBOUNCE_MS (600), UNDO_STACK_MAX (10) in lib/constants.ts. (2) **project-storage:** getGraphStateForUndo(nodes, edges) returns deep-cloned state with initialContent from getModel for computation nodes; syncModelsToGraphNodes(nodes) syncs Monaco models after undo/redo. (3) **graph-store:** undoStack, redoStack, setUndoSnapshotGetter; addNode, moveNode, removeNode, addEdge, removeEdge, setNodeInputs push snapshot via getter (when set) and clear redo; setGraph(nodes, edges, { clearHistory?: boolean }); undo()/redo() with deep clone. (4) **Toolbar:** Save button removed; Undo/Redo buttons (data-testid undo-btn, redo-btn), disabled when stack empty, call syncModelsToGraphNodes after undo/redo. (5) **Canvas page:** Debounced save (performSave + requestSave), AutoSaveContext (from contexts/auto-save-context.tsx), getter registration and cleanup on unmount, keydown Ctrl/Cmd+Z and Shift+Ctrl/Cmd+Z. (6) **Editor:** useAutoSave().requestSave() in model.onDidChangeContent. (7) **use-project-loader:** setGraph(..., { clearHistory: true }). (8) **E2E:** Toolbar asserts Undo/Redo (no Save); tests for Undo removes node, Redo restores node. (9) **Unit tests:** graph-store (getter not set, push+undo+redo, clearHistory, max stack); project-storage (getGraphStateForUndo, syncModelsToGraphNodes); constants. (10) **memory/systemPatterns:** data-testid list (undo-btn, redo-btn; no save-btn), auto-save and undo/redo bullets. pnpm test (199 frontend), pnpm run lint, pnpm build, pnpm smoketest (24) pass.
- **Review and improve (canvas drag):** (1) **node-interaction-shell:** File-level JSDoc describing drag zone vs content zone; NodeFrame.displayName for DevTools. (2) **E2E:** Extracted assertNodeStationaryAfter(page, nodeTestId, action) and assertNodeMovesAfterDrag(page, nodeTestId, dragZoneTestId, deltaX, deltaY); all six drag/click tests use them; position assertions use toBeCloseTo(..., 0) for subpixel tolerance. (3) **Docs:** COMPUTATION_BLOCKS.md — new "Moving blocks" section (drag by title bar; editor/inputs clickable without moving). pnpm test, pnpm run lint pass.
- **Canvas drag interaction redesign:** (1) **Shared node interaction shell** in `node-interaction-shell.tsx`: `NodeFrame` (nopan root, drag zone with getNodeDragHandleClassName, nodeTestId/titleTestId), `NodeContentZone` (NODRAG_CLASS + optional NOWHEEL_CLASS, forwardRef). (2) **Constants:** NOWHEEL_CLASS in lib/constants.ts; constants.test asserts NODRAG_CLASS and NOWHEEL_CLASS. (3) **Computation and presentation nodes** refactored to use NodeFrame + NodeContentZone; all editor/inputs/result inside ContentZones; data-testid computation-drag-zone, computation-editor-zone. (4) **Drag policy:** graph-drag-handle.ts getDragHandleSelector(type) uses getNodeDragHandleClassName from shell; graph-canvas uses getDragHandleSelector(n.type). (5) **Unit tests:** node-interaction-shell.test.tsx (getNodeDragHandleClassName, NodeFrame/NodeContentZone classes), graph-drag-handle.test.ts. (6) **E2E:** canvas.spec.ts — "Clicking input controls does not drag", "Dragging from drag zone moves the computation node", "Clicking editor zone keeps the computation node stationary" (getNodeRect for position). pnpm test (181 frontend), pnpm run lint, pnpm build pass; run pnpm smoketest for full E2E.
- **Review and improve (computation box + drag/nodrag):** (1) **NODRAG_CLASS constant** in lib/constants.ts (value `'nodrag'`) and used in ComputationBoxNode and PresentationBlockNode so we have one place to change if React Flow renames it. (2) **Computation box** uses NODRAG_CLASS on Inputs label, inputs form, editor wrapper, and result section; node root has `cursor: 'default'`. (3) **Presentation block** has `cursor: 'default'` on root and wraps content in a div with NODRAG_CLASS for consistency. (4) **systemPatterns** Canvas node drag bullet updated: use NODRAG_CLASS on all non-handle content and node root cursor default so grab cursor only on handle. (5) **Constants test** asserts NODRAG_CLASS. (6) **E2E** new test: "Computation node shows Add input button (inputs UI is present)" — adds computation node, asserts computation-add-input is attached. pnpm test (178 frontend), pnpm run lint, pnpm smoketest (17) pass.

## Just completed (earlier)
- **Inputs as block properties (not in block text):** (1) **Data model:** Inputs are stored on the computation node (`node.inputs`) and persisted in ProjectSnapshot (`ProjectNode.inputs`). (2) **Graph store:** `setNodeInputs(nodeId, inputs)`; `applyNodeSignature` now only updates `outputType` (LSP no longer overwrites inputs). (3) **Persistence:** types/project.ts has `ProjectNodeInput` and parses/serializes `inputs`; buildSnapshotFromGraph includes inputs for computation nodes; snapshotToGraphNodes loads inputs when loading a project. (4) **UI:** Computation box has an editable Inputs section: name (text), type (dropdown: Vector, Numeric, etc.), remove (×), and "+ Add input". Handles render only for inputs with non-empty name. (5) **Docs:** COMPUTATION_BLOCKS.md updated — inputs are block properties edited in the UI; use `$name` in the script. (6) **Constants:** INPUT_PORT_TYPES in lib/constants.ts. (7) **Tests:** graph-store (applyNodeSignature no longer sets inputs, setNodeInputs test), project types (parse inputs), project-storage (buildSnapshot includes inputs), project-loader-utils (snapshotToGraphNodes maps inputs), constants (INPUT_PORT_TYPES). pnpm test (176 frontend), pnpm run lint pass.
- **Autonomous verification (add-node + selection loop):** (1) **Selection callback:** project.$projectId.tsx now passes a stable `handleSelectionChange` (useCallback, deps []) to GraphCanvas to fix "Maximum update depth exceeded" from React Flow's SelectionListener. (2) **Full verification:** pnpm test (backend + frontend 166), pnpm run lint (clippy + ESLint), pnpm build, pnpm smoketest (16 E2E vs preview), pnpm smoketest:dev (16 E2E vs dev server) all pass. Ensure port 3000 is free when running `pnpm smoketest` (it starts preview); use `pnpm smoketest:dev` when dev server is already running.
- **Add computation/presentation block not showing (dev):** (1) **Loader race:** use-project-loader now runs fully synchronously (no async IIFE) so setGraph happens before first paint. (2) **Strict Mode re-run:** Added lastLoadedProjectIdRef guard so when the effect re-runs for the same projectId (e.g. React Strict Mode in dev), we skip dispose/setGraph/clearAll and do not overwrite user-added nodes. (3) **Dev smoketest:** Run `pnpm dev` in one terminal, then `pnpm smoketest:dev` in another to run E2E against the dev server (PLAYWRIGHT_REUSE_SERVER=1, no webServer). All 16 E2E pass against dev including add computation/presentation node. pnpm test:frontend, pnpm run lint:frontend pass.
- **UI overhaul and robustness:** (1) **Global styles:** Added `src/app.css` with CSS variables (--bg-primary, --accent, --text-*, --font-sans, --font-mono, --radius, --shadow). Root imports app.css; Inter + JetBrains Mono loaded via root head links. (2) **Block visibility:** ComputationBoxNode IntersectionObserver rootMargin increased to 300px so editor stays mounted when nodes are near viewport. (3) **Project list:** Uses design tokens, max-width 640, clearer typography and buttons (accent for New/Open). (4) **Toolbar:** Design tokens, "← Back", flex spacer, accent for Add computation, danger-soft for Delete selected. (5) **Node cards:** Computation and presentation nodes use --bg-card, --border, --shadow, var(--radius). (6) **Toast display:** New ToastList component in root; toasts from ui-store shown bottom-right with dismiss. (7) **Canvas/graph:** Page and graph canvas use --bg-primary; fitViewOptions padding 0.2, maxZoom 1.2. (8) **Widget/Presentation:** WidgetDataView and presentation placeholder use CSS vars. pnpm run lint, pnpm run test:frontend (166), pnpm run smoketest (16) pass.
- **Review and improve (E2E again):** (1) **Rename E2E:** New test renames project via prompt dialog, goes back to list, asserts new name visible. (2) **Import E2E:** data-testid="import-file-input" on file input; e2e/fixtures/valid-project.snaq.json (minimal snapshot); test sets file and asserts navigation to canvas. (3) **gotoCanvas:** Use `Page` type from `@playwright/test` instead of inline import. (4) **systemPatterns:** Added import-file-input to data-testid list. pnpm test (166), pnpm run smoketest (16), pnpm run lint pass.
- **Review and improve (E2E + selection):** (1) **E2E DRY:** Added `gotoCanvas(page)` helper in canvas.spec.ts; all canvas tests use it. (2) **Export assertion:** E2E now asserts download filename matches `project-<uuid>.snaq.json` (regex). (3) **Unit test:** project-list-sort.test.ts verifies sort preserves other properties (id, name) of each item. (4) **Delete selected E2E:** New test adds computation node, clicks Delete selected, asserts node count 0. (5) **Selection sync:** GraphCanvas accepts `selectedNodeIds` and merges `selected: set.has(n.id)` into nodes so React Flow selection matches app state; project route passes selectedNodeIds. (6) **New node selected:** handleAddNode now calls setSelectedNodeIds([id]) so newly added node is selected and Delete selected works without clicking the node (avoids canvas visibility/click issues in E2E). pnpm test (166), pnpm run smoketest (14), pnpm run lint pass.
- **Agent UI observation and Playwright:** (1) **Playwright:** Added @playwright/test in apps/frontend; playwright.config.ts (baseURL 3000, testDir e2e, webServer when !PLAYWRIGHT_REUSE_SERVER, trace on-first-retry). Scripts: smoketest (playwright test), smoketest:dev (REUSE_SERVER=1). Root: pnpm smoketest (build:frontend + smoketest), pnpm smoketest:dev. Vite preview port set to 3000 so webServer matches baseURL. (2) **E2E specs:** e2e/project-list.spec.ts (project list page, New project button, click → canvas with UUID); e2e/canvas.spec.ts (toolbar and graph after New project, Back to projects). (3) **data-testid:** project-list-page, new-project-btn, project-list (index); canvas-toolbar, back-to-projects, add-computation-btn (toolbar); canvas-page, graph-canvas-wrapper (project route). (4) **Artifacts:** e2e-artifacts/ in .gitignore; apps/frontend/test-results/ for Playwright. (5) **Docs:** memory/systemPatterns (E2E/smoketest commands, agent browser MCP workflow); docs/agent-ui-workflow.md (run app, smoketest commands, screenshot path, MCP lock/snapshot/screenshot/unlock, detect→implement→review). (6) **.cursorrules:** 1.3 — when changing UI or user-facing flows, run pnpm smoketest (or smoketest:dev). (7) **CI:** Install Playwright browsers (chromium), then pnpm run smoketest. (8) **README:** Frontend E2E (Playwright) section and first-time playwright install. ESLint ignores e2e/ so specs are not type-checked by main tsconfig. pnpm test, pnpm run lint, pnpm smoketest pass.
-
- **Review and improve (Projects/canvases):** (1) **Single source of truth for storage keys:** Exported PROJECTS_INDEX_KEY and PROJECT_KEY_PREFIX from project-storage; project-storage.test.ts, export-import.test.ts, and projects-index-store.test.ts import and use them. (2) **Toolbar DRY:** getCurrentSnapshot(projectId) helper in project-toolbar builds snapshot from graph store; saveCurrent and handleExport use it. (3) **Project list sort:** Index page shows projects sorted by updatedAt descending (useMemo). (4) **Import error UX:** Index page uses useUIStore.getState().addToast(result.error, 'error') instead of alert() when import fails. (5) **README:** Structure bullet for apps/frontend now mentions multiple projects (create, rename, delete, export/import) and canvas of computation/presentation blocks. pnpm test (161), pnpm run lint pass.
- **Test coverage for Projects/canvases:** (1) **project-storage.test.ts:** getProjectsIndex/setProjectsIndex (empty, invalid JSON, filter invalid entries); getProjectSnapshot/setProjectSnapshot/deleteProjectSnapshot (round-trip, missing, invalid shape); buildSnapshotFromGraph (initialContent, empty content, presentation nodes). (2) **export-import.test.ts:** buildDownloadBlob (blob type/filename/size, pretty-print); importProjectFile (valid file → new UUID + persist, invalid JSON, invalid format, read error). (3) **project-loader-utils.ts** (extracted from use-project-loader): isValidUuid, snapshotToGraphNodes; **project-loader-utils.test.ts** (UUID accept/reject, snapshot→graph nodes with content/uri). (4) **projects-index-store.test.ts:** removeProject deletes project snapshot from localStorage. (5) **graph-store.test.ts:** setGraph preserves initialContent. Export: buildDownloadBlob() added for testable download logic. pnpm test (161 frontend), pnpm run lint pass.
- **Projects and Canvases:** (1) **Types:** ProjectSnapshot, ProjectNode, ProjectEdge, parseProjectSnapshot in types/project.ts. (2) **Stores:** graph-store setGraph and GraphNode.initialContent; widget-store clearAll; projects-index-store (hydrate, addProject, removeProject, updateProjectName) with localStorage. (3) **Persistence:** project-storage (get/set index and snapshot, buildSnapshotFromGraph, deleteProjectSnapshot). (4) **Router:** `/` = project list (New project, Import, Open, Delete); `/project/$projectId` = canvas with useProjectLoader, ProjectToolbar, GraphCanvas. (5) **Loader:** use-project-loader disposes current models, loads snapshot, setGraph with initialContent, clearAll widgets; invalid projectId redirect to `/`; race handled with requestedRef. (6) **Toolbar:** Back, Save, Export, Add computation/presentation, Delete selected, Rename, Delete project. (7) **Export/import:** downloadProjectSnapshot; importProjectFile parses JSON, validates, creates new project with new UUID. (8) **Add/remove blocks:** Toolbar buttons + Delete/Backspace key; GraphCanvas onSelectionChange; ComputationBoxEditor receives node.initialContent. (9) **Tests:** setGraph, clearAll, parseProjectSnapshot, projects-index-store (hydrate, add, remove, updateName). (10) **Docs:** systemPatterns updated with projects/canvases. pnpm test (132 frontend), pnpm run lint, pnpm build (frontend) pass.
- **Presentation Blocks (Live Subscriptions, Section 5):** (1) **Widget store:** setWidget merges Running payload.elements (append) when current and incoming are both Running with elements; terminal status (Completed/Cancelled/Error) replaces state. mergeWidgetState helper; unit tests for append, Completed replace, empty elements. (2) **Graph canvas:** graphNodeToFlowNode takes storeNodes and storeEdges; for type===presentation, sourceUri derived from edge (source node URI), else ''; nodes useMemo depends on storeNodes and storeEdges. (3) **Presentation block:** Subscribe only when sourceUri non-empty (isWired); early return no-op cleanup when !isWired; render placeholder "Connect a computation box" when unwired, WidgetDataView when wired. (4) **Docs/tests:** systemPatterns updated (sourceUri from edge, widget store append); presentation-block.test.tsx added (does not call subscribeWidget when sourceUri empty). pnpm test (109 frontend), pnpm run lint, pnpm build (frontend) pass.
- **Review and improve (LSP / Monaco Section 4):** (1) **client.ts:** Removed dead code: request(), sendNotification(), processRawMessage(), and onLspResponse handler registration. client.ts now only exports initLspClient(workerUrl, wasmUrl?) which calls initMessageRouter. All LSP traffic goes through getLanguageClient() (LspProvider sets the singleton after connection handshake). (2) **lsp/index.ts:** Dropped exports of request, sendNotification, processRawMessage; added export of processIncomingMessage from message-router for discoverability. (3) **LspProvider:** startConnection() wrapped in try/catch; errors logged with console.error('[LSP] Failed to start connection:', err). (4) **Presentation-block:** When the language client is not ready on mount, retries subscribe every 200ms for up to 10s; clears interval on cleanup and unsubscribes only if we ever subscribed. (5) **Memory/docs:** activeContext updated. pnpm test (105 frontend), pnpm run lint, pnpm build (frontend) pass.
- **Monaco Editor inside Virtual Documents (Section 4):** (1) **Deps:** vscode-languageclient and vscode-jsonrpc added (monaco-languageclient skipped due to peer dep conflict with monaco-editor). (2) **Transport:** waitForWorkerReady(), setIncomingLspPush(); PushMessageReader and WorkerMessageWriter in lsp-connection.ts; createLspConnection(send) returns connection + push; message router calls incomingLspPush(raw) after routeMessage. (3) **LspProvider:** After waitForWorkerReady(), creates connection, connection.listen(), initialize/initialized handshake, setIncomingLspPush(push), setLanguageClient({ sendRequest, sendNotification }). (4) **Singleton:** getLanguageClient() / setLanguageClient() / hasLanguageClient() in language-client-singleton.ts; edge-handlers and presentation-block use getLanguageClient().sendRequest(). (5) **Editor:** ComputationBoxEditor uses hasLanguageClient/getLanguageClient for didOpen/didChange; cleanup disposes only editor (not model). (6) **Model lifecycle:** LspProvider subscribes to nodes and calls disposeModel(nodeIdToUri(id)) when a node is removed. (7) **Diagnostics:** onPublishDiagnostics handler; applyDiagnosticsToMonaco in editor/apply-diagnostics.ts (setModelMarkers); route-message dispatches to handler (no monaco import in route-message for testability). (8) **ComputationBoxNode:** ResizeObserver for editor dimensions; IntersectionObserver for in-view; placeholder when not in view. (9) **Tests:** waitForWorkerReady in message-router.test.ts; publishDiagnostics dispatch in route-message.test.ts; edge-handlers mock getLanguageClient. (10) **Docs:** systemPatterns.md and LSP.md updated. pnpm test (79), pnpm run lint, pnpm build (frontend) pass.
- **Canvas and Node State Management (Section 3):** (1) **disconnectEdge:** Optimistic remove from store, then request(LSP_METHOD_GRAPH_DISCONNECT, { targetUri, targetInputName }); on LSP error rollback (addEdge) and addToast. (2) **GraphCanvas:** onEdgesDelete → for each deleted edge resolve targetUri (store node by edge.target), targetInputName = edge.targetHandle, call disconnectEdge. (3) **ComputationBoxNode:** Input handles positioned at top: 32 + i * 24; minHeight = Math.max(120, 32 + inputs.length * 24 + 16). (4) **Tests:** edge-handlers.test.ts (vi.mock ~/lsp) — disconnectEdge calls LSP with correct params; on error rollback + toast; no-op when target node missing. (5) **Docs:** systemPatterns — edge deletion triggers disconnectEdge and LSP disconnect for widget invalidation; LSP.md — UI triggers disconnect on edge delete (e.g. Backspace). pnpm test, pnpm run lint, pnpm build (frontend) pass.
- **Review and improve (Worker/LSP bridge):** (1) **DRY:** Worker control message types (init, ready, error) moved to lib/constants.ts (WORKER_MSG_INIT, WORKER_MSG_READY, WORKER_MSG_ERROR); message-router and worker use them; constants.test.ts asserts values. (2) **LSP.md:** Manual wasm-pack command uses --out-dir ../../apps/frontend/public/lsp-wasm (relative to crate). (3) **Worker:** Ignore duplicate init when wasmLoading or wasmReady; wasmLoading flag and finally clear. (4) **Test:** message-router worker-error test mocks console.error and restores. (5) **LspProvider:** Comment that wasmUrl is browser-only. (6) **ESLint:** Ignore public/lsp-wasm/** so generated wasm-pack output is not linted. pnpm test, pnpm run lint, pnpm build (frontend) pass.
- **Spawn Web Worker; LSP runs in worker:** (1) **Backend:** snaq-lite-lsp Cargo.toml: crate-type = ["cdylib", "rlib"] for wasm-pack; uuid "js" feature for wasm32; fixed params.widget_id move in subscribeWidget WASM branch. (2) **Build:** Root script `build:lsp-wasm` (cargo build wasm32 --lib + wasm-pack --out-dir ../../apps/frontend/public/lsp-wasm); .gitignore apps/frontend/public/lsp-wasm/. (3) **Worker:** lsp.worker.ts handles first message { type: 'init', wasmUrl }; dynamic import(wasmUrl), default(), start_snaq_lite_lsp(postMessageCallback); buffer until ready; post snaqlite-worker-ready / snaqlite-worker-error. (4) **Router:** initMessageRouter(workerUrl, wasmUrl); send init as first message; processIncomingWorkerMessage (ready/error sets workerReady); resetMessageRouterForTest for tests. (5) **Client/Provider:** initLspClient(workerUrl, wasmUrl?); LspProvider computes wasmUrl from origin + BASE_URL + lsp-wasm/snaq_lite_lsp.js. (6) **Tests:** message-router.test.ts (processIncomingWorkerMessage ready/error/LSP passthrough, invalid JSON). (7) **Docs:** LSP.md — build:lsp-wasm, output path, init/ready/error protocol. pnpm test, pnpm run lint, pnpm run build:lsp-wasm pass.
- **Review and improve (docs + root package):** (1) **Root package.json** formatted with newlines for readability. (2) **README.md:** Structure now lists apps/frontend (TanStack Start SPA, pnpm). Run tests section explains root `pnpm test` (after install:frontend), backend-only and frontend-only commands, and `pnpm run lint`. Build and run adds frontend dev/build/preview.
- **ESLint + CI (frontend):** (1) **ESLint:** apps/frontend — flat config (eslint.config.js), @eslint/js + typescript-eslint + react + react-hooks, recommended rules; `lint` / `lint:fix` run `eslint . --max-warnings 0`. Fixed no-unused-vars (PresentationBlockNode `id`, getModel `_monaco`); argsIgnorePattern/varsIgnorePattern `^_`. (2) **Lint-rule tests:** src/eslint/lint-rules.test.ts (no-unused-vars fires for unused var; underscore-prefixed allowed); test-eslint.config.js for programmatic tests. (3) **Root:** `lint:frontend`, `lint` = lint:backend && lint:frontend. (4) **CI:** pnpm/action-setup@v4 (v10), install:frontend; separate steps: Run backend tests, Run clippy, Run WASM tests, Lint frontend, Test frontend. Node cache: pnpm.
- **Review and improve (root scripts + .gitignore):** (1) **Root package.json:** Formatted for readability; added `install:frontend` (pnpm -C apps/frontend install) for one-command setup from root; added `lint` / `lint:backend` (cargo clippy --workspace) for quick lint without full test. (2) **.gitignore:** Top comment clarifies that pnpm-lock.yaml is committed (only npm lockfiles and backup ignored).
- **Review and improve (frontend):** (1) **Cleanup:** Removed unused `useCallback` from computation-box-editor. (2) **route-message:** Added `clearMessageRouterHandlers()` for test isolation; route-message tests use it in `beforeEach`. (3) **Tests:** New `lib/constants.test.ts` (VIRTUAL_URI_PREFIX, LSP method names); lsp/types edge case: `isRequest` false when both method and result present. (4) **Code quality:** .cursorrules 1.1 — when changing apps/frontend, run `pnpm test` and `pnpm build` there. 47 frontend tests, 9 files.
- **Frontend test coverage:** Vitest + jsdom in apps/frontend; `pnpm test` / `pnpm test:watch`. Tests: editor/virtual-uri (nodeIdToUri, uriToNodeId, isGraphNodeUri), lsp/types (isRequest, isResponse, isNotification), lsp/route-message (routeMessage dispatches response/nodeSignatureUpdated/widgetDataUpdate, ignores invalid JSON), store/graph-store (addNode, moveNode, removeNode, addEdge, removeEdge, setPendingEdge, clearPendingEdge, applyNodeSignature), store/ui-store (addToast, removeToast), store/widget-store (setWidget, removeWidget), lib/utils (generateWidgetId), api/client (getBaseUrl, apiFetch). Vite config uses VITEST to skip TanStack Start plugin in test runs. 44 tests, 8 files.
- **Frontend UI and WASM LSP (structure and scaffold):** Implemented plan under `apps/frontend/`. (1) **TanStack Start SPA:** Vite 7, tanstackStart({ spa: { enabled: true } }), router, __root, index route, entry-client with StartClient. (2) **Worker + LSP bridge:** lsp.worker.ts (stub responds to initialize), message-router (postMessage/onmessage, route by method/id), route-message (nodeSignatureUpdated → store, widgetDataUpdate → store), lsp/types.ts (JSON-RPC + custom params), lsp/client.ts (request, sendNotification, initLspClient). (3) **Graph store + canvas:** Zustand graph-store (nodes, edges, pendingEdge, applyNodeSignature), ui-store, widget-store; React Flow graph-canvas, computation-box-node, presentation-block-node, edge-handlers (connectEdge → LSP connect + toast on error). (4) **Monaco:** editor/virtual-uri (nodeIdToUri, uriToNodeId), text-model-registry, monaco-language-client (transport stub), computation-box-editor (didOpen/didChange via router). (5) **Presentation:** PresentationBlock (subscribeWidget on mount, unsubscribeWidget on unmount), WidgetDataView, LspProvider (init worker + setMessageRouterHandlers for graph + widget). (6) **API + lib:** api/client.ts (getBaseUrl, apiFetch), lib/constants (VIRTUAL_URI_PREFIX, LSP method names), lib/utils (generateWidgetId), .env.example (VITE_API_BASE_URL). No SSR or server routes; backend remains independent.
- **Review and improve (LSP graph/widget test coverage):** (1) **LSP.md:** Document that widgetDataUpdate notifications are flushed when the server handles the next request (hover, inlay hint, or didOpen/didChange). (2) **inlay_hint:** Call drain_widget_notifications at start (same as hover) so any common request type flushes pending widget updates. (3) **Wired widget test:** Named constant WIDGET_DRAIN_DELAY_MS (150) with comment; doc comment aligned with assertion ("totalElements ≥1 confirms graph wiring"); expect() instead of unwrap() for params object with clear message. cargo test --workspace, clippy pass.
- **Test coverage for LSP graph/widget:** (1) **Lexer:** Reset `after_postfix_factor` on Newline so `"input x: Vector\n[1, 2]"` parses (next line `[` is vector literal, not BracketKey). (2) **Lang:** Unit test `extract_input_decls_single_input_and_vector_literal` for `"input x: Vector\n[1, 2]"` → one input (x, Vector). (3) **LSP:** Drain widget notifications at start of `hover` so client receives `widgetDataUpdate` after subscribeWidget (background consumer sends to channel; drain on next request). (4) **Integration:** `native_node_signature_updated_after_did_open` now passes (lexer fix). `native_graph_wired_widget_gets_downstream_result`: target document uses `$x` (not `x`) to reference wired stream; send hover after subscribe to trigger drain; 150ms sleep before hover; assert `totalElements >= 1` (confirms vector path and wiring). (5) **Disconnect:** `native_graph_disconnect_removes_edge` already passing. All 16 LSP integration + 37 LSP unit tests pass; cargo test --workspace, clippy --workspace, wasm-pack test --node crates/snaq-lite-wasm pass.
- **Visual Computation Graph via LSP (plan):** (1) **Virtual documents:** Multi-document state (URI → DocumentEntry), didOpen/didChange keyed by URI; diagnostics/hover/subscribe use per-URI lookup. (2) **Declarative inputs:** Grammar `input name: Type`; IR InputDecl; `extract_input_decls_from_block`, `value_type_name` in lang/graph.rs; LSP sends `snaqlite/graph/nodeSignatureUpdated` after didOpen/didChange. (3) **Graph + connect:** GraphState (edges, connect, disconnect, incoming, topological_order); `snaqlite/graph/connect` with type check (exact type name); `snaqlite/graph/disconnect` (targetUri, targetInputName). (4) **Widgets:** subscribeWidget (run source, Vector → stream consumer thread, scalar → one Completed); widgetDataUpdate (Running/Completed/Cancelled/Error); unsubscribeWidget; WidgetRegistry; drain_widget_notifications in didOpen/didChange. (5) **Reactive invalidation:** didOpen/didChange → invalidate_widgets_for_uri; disconnect → invalidate_widgets_for_uri(target). (6) **Docs:** LSP.md — "Visual computation graph" (virtual URIs, nodeSignatureUpdated, connect, disconnect, subscribeWidget, widgetDataUpdate, unsubscribeWidget, invalidation). (7) **Tests:** Unit — graph (connect, disconnect, topological_order), widget_registry; integration — multi_document_hover_two_virtual_uris, graph_connect_success, graph_connect_type_mismatch_returns_error, subscribe_widget_scalar_returns_completed, subscribe_widget_then_unsubscribe_receives_cancelled, subscribe_widget_then_did_change_receives_cancelled. cargo test --workspace, clippy --workspace, wasm-pack test --node crates/snaq-lite-wasm pass.
- **Fix CLI stream-mode hangs:** (1) **Tests:** `.stdin(Stdio::null())` in common.rs so child CLI never blocks on stdin. (2) **File sync:** `write_and_sync()` in common.rs; cli_file_formats and cli_stream_integration use it for all temp files used as `--stream` paths. (3) **Feeder-ready:** Optional `on_ready: Option<Box<dyn FnOnce() + Send>>` in feed_stream_file_to_sender and feed_file_to_sender (stream_feeder); feeder signals after opening file; main waits for all ready before proceeding. (4) **Root cause:** Hang was in lang's value() calling collect_vector_stream before feeders ran. **Fix:** CLI now spawns feeder threads and waits for ready **before** calling run_with_stream_inputs, so when value() does block_on(stream.collect()) data is already being sent. (5) **Vector branch:** Use take_evaluated_results() when available, else vector_into_stream+collect; join feeders after run_with_stream_inputs; on error join feeders before exit. (6) **Display:** Value::Undefined in stream vector output shows "?". (7) **Parquet/Arrow tests:** Temp paths use .with_extension("parquet")/.with_extension("arrow") so format is detected. (8) **Timeout panic:** common.rs includes stderr/stdout in panic when CLI subprocess times out. All 45 CLI tests pass (no timeouts); cargo test --workspace, clippy, wasm-pack test pass.
- **ExprReset workaround extended to all LALRPOP #596 contexts:** (1) **VecExprList:** vector literal elements use `ExprReset` so `[1 < 2, 1 == 2]` etc. parse; un-ignored run_format_vector_of_booleans, run_vector_all_any. (2) **Parentheses:** Factor and ImplicitMulRight `(expr)` use `ExprReset`; un-ignored run_runtime_error_shows_location_and_snippet, run_runtime_error_multiline_includes_location_and_message. (3) **BlockItem:** standalone expression and binding RHS use `ExprReset` so top-level and comparison RHS accept full expressions; un-ignored parse_comparison and all run_comparison_* tests. (4) **Lexer:** after emitting `==`, `!=`, `<=`, `>=` set `after_postfix_factor = false` so RHS vector literals (e.g. `[1,0,3] == [1,0,3]`) are not lexed as BracketKey. (5) **Docs:** SYNTAX.md — Known parser limitation now states all these contexts are fixed; remaining nested positions may still be limited. No remaining `#[ignore = "parser limitation (LALRPOP #596)"]` tests. cargo test, clippy --workspace, wasm-pack test pass.
- **ExprReset workaround for if/then/else (LALRPOP #596):** (1) **Grammar:** IfExpr now uses `ExprReset` (not `Expr`) for cond, then_b, else_b so condition and branches accept full expressions. Added `ExprReset` rule (after AddExpr) with same alternatives as Expr but without precedence: IfExpr, 8 chained comparisons, 6 single comparisons, AddExpr. (2) **Lexer:** After emitting `if`, `then`, or `else`, set `after_postfix_factor = false` so the next token (e.g. `[1, 2]` in else branch) is not lexed as BracketKey. (3) **Tests:** Un-ignored 10 if/then/else tests (parse_if_then_else, run_if_crisp_then/else, run_if_superposition_*, run_if_branch_type_mismatch, run_if_expected_condition, run_if_expected_condition_includes_location_when_source_provided). (4) **Docs:** SYNTAX.md — Known parser limitation notes if/then/else fixed; comparison and vector literal contexts still limited. activeContext — this bullet. Comparison/top-level and vector literal tests remain ignored. cargo test, clippy --workspace, wasm-pack test pass.
- **Review and improve (STEM test coverage):** (1) **Section comment:** Added a short header for the STEM tests block listing volume, pressure, mass, temperature, energy, power, concentration, percent/ppm/ppb, angles, knot/nmi, steradian. (2) **Doc comments:** Added `///` docs for volume_cm3_mm3_prefix_resolution, stem_molar_m_alias_parses_and_equals_molar, stem_percent_ppm_ppb_run_numeric, stem_nautical_mile_convert, stem_sr_alias_parses, and run_numeric_physical_constants_c_h_r (lib.rs). (3) **Percent conversion:** stem_percent_ppm_ppb_run_numeric now asserts 50 percent as scalar = 0.5 in addition to 100 percent = 1. cargo test --workspace, clippy pass.
- **STEM units and constants:** (1) **Volume:** dimension Volume; m3 (m³, cubicmeter), km3/cm3/mm3 via CUBED_LENGTH_REST in try_lookup; liter (litre, L), milliliter (ml, mL). (2) **Pressure:** bar, atm, psi, mmHg, torr. (3) **Mass:** pound (lb), ounce (oz). (4) **Temperature:** fahrenheit (scale 5/9 for differences; absolute zero not modeled). (5) **Energy:** calorie, kcal, BTU, Wh, kWh. (6) **Power:** horsepower (hp). (7) **Concentration:** molar (M), mol/L. (8) **Dimensionless:** percent, ppm, ppb. (9) **Angle:** arcmin (arcminute), arcsec (arcsecond). (10) **Velocity:** nautical_mile (nmi), knot; base dimension Velocity. (11) **Solid angle:** steradian, sr. (12) **Symbol registry:** c, h, hbar, R in default_registry(). Docs: UNITS_AND_QUANTITIES.md (overview + Temperature subsection), README (STEM-friendly line), SYMBOLS.md (physical constants). Memory: systemPatterns (Units bullet), activeContext. Tests: volume_*, stem_pressure_*, stem_pound_*, stem_fahrenheit_*, stem_calorie_*, stem_horsepower_*, stem_molar_*, stem_percent_*, stem_arcmin_*, stem_knot_*, stem_steradian_*, default_has_physical_constants, run_numeric_physical_constants_c_h_r. numbat_units_we_do_not_support_treated_as_symbols now uses "stone". **Extended test coverage:** volume_cm3_mm3_prefix_resolution; stem_pressure extended with psi, mmHg, torr; stem_calorie extended with kcal, BTU, kWh; stem_molar_m_alias_parses_and_equals_molar; stem_percent_ppm_ppb_run_numeric; stem_nautical_mile_convert; stem_sr_alias_parses; run_numeric_physical_constants_c_h_r extended with h and hbar. cargo test --workspace, clippy, wasm-pack test pass.
- **Review and improve (CLI integration tests):** (1) **DRY:** Added `unique_tmp_csv(prefix)` so CSV tests use one helper and never forget `.with_extension("csv")`. (2) **Format detection:** Test `cli_numeric_no_extension_treated_as_numeric_lines` — path with no extension (e.g. `.data`) is treated as numeric lines, stdout `[1, 2]`. (3) **Module doc:** Explained why some tests are `#[ignore]` (subprocess hang in some envs; behaviour covered by stream_feed_dispatch/stream_feeder unit tests). cargo test --workspace, clippy, wasm-pack test pass.
- **CLI file-format integration tests:** (1) **New test file:** `crates/snaq-lite-cli/tests/cli_file_formats_integration.rs` with `run_cli(args)`, `cli_bin()`, `unique_tmp_path()`, `unique_tmp_csv()`. (2) **Numeric (.txt):** empty file → `[]`; single value; multiple `$x * 2` → `[2, 4, 6]`; no-extension path → numeric lines; blank lines skipped; large file chunking sum (ignored); `--numeric` one per line. (3) **CSV:** headers-only → `[]`; single/multi row, empty cell, chunking, `--numeric` (ignored). (4) **Parquet/Arrow:** `#[cfg(feature = "parquet")]` module; write temp .parquet/.arrow; cli_parquet_mapped_column, cli_arrow_mapped_column (ignored). (5) **Errors:** file not found; invalid numeric line; invalid CSV cell (ignored). (6) **Args/usage:** missing expression, --stream without name=path, invalid --stream-variance, empty name/path. (7) **Non-stream:** simple expression, --numeric, parse error, symbolic output, function → "<function>"; stream mode scalar result; unbound stream name. (8) **Cargo.toml:** default = ["parquet"]; dev-deps arrow, arrow-ipc, parquet. (9) **Ingest:** Removed redundant `#![cfg(feature = "parquet")]` for clippy.
- **Live pub-sub LSP results (snaqlite/*):** (1) **Custom methods:** `snaqlite/subscribe` (params: textDocument.uri, optional range; returns subscriptionId), `snaqlite/unsubscribe` (subscriptionId). (2) **Notification:** `snaqlite/publishResult` (server→client) with status Running/Completed/Error/Cancelled and data per stream-chunk protocol (elements with display/undefined/error, offset, count). (3) **Subscription registry:** subscription.rs (insert, remove, invalidate_uri); cancel via oneshot; shared state + notification channel (UnboundedSender to backend, drain_notifications on handlers). (4) **Subscribe handler:** root-only; run_with_stream_inputs; if Vector → spawn native consumer thread (run_stream_consumer with LocalPool, batch 100, send Running then Completed); scalar/WASM → single Completed. (5) **Lifecycle:** did_change/did_open call invalidate_subscriptions_for_uri (cancel + publishResult Cancelled). (6) **LspService::build()** with custom_method for subscribe/unsubscribe; PublishResultNotification (lsp_types) for send_notification. (7) **Docs:** docs/LSP.md — Pub-sub section, methods table, data wire format, lifecycle. (8) **Tests:** subscription_registry_insert_remove, invalidate_uri; stream_element_to_json_error/undefined. cargo test, clippy --workspace, wasm-pack test pass.
- **Pub-sub test coverage:** Unit: subscription_registry_invalidate_all; stream_element_to_json_ok_some_value; subscribe_params_deserializes_camel_case, subscribe_response_serializes_camel_case, unsubscribe_params_deserializes_camel_case. Integration: native_subscribe_scalar_returns_id_and_completed; native_subscribe_vector_returns_id_and_stream_completes; native_subscribe_then_unsubscribe_succeeds; native_subscribe_then_did_change_receives_cancelled. send_init_and_initialized(client_w, client_r) for init flow. 23 LSP unit + 6 integration tests pass.
- **Review and improve (pub-sub tests):** (1) **DRY:** TEST_DOC_URI, DUPLEX_BUFFER_SIZE constants; open_document(client_w, text, version) helper; all pub-sub tests use send_init_and_initialized + open_document. (2) **Scalar test:** Loop until both subscription_id and Completed received (order-independent). (3) **Vector test:** Removed no-op block; use expect() for JSON parse so malformed messages fail fast. (4) **Unit:** publish_result_params_status_serializes_pascal_case (PublishStatus round-trip). (5) **Integration:** native_subscribe_wrong_uri_returns_error (subscribe with file:///other.snaq when doc is file:///doc.snaq → invalid_params). (6) **Docs:** LSP.md Verification lists pub-sub integration tests. 24 unit + 7 integration tests; clippy clean.
- **Dual-target LSP server (snaq-lite-lsp):** (1) **Crate:** snaq-lite-lsp with lib (SnaqLiteBackend, LspState, mapping span→range, run_error→diagnostic), native binary (main.rs, Tokio stdin/stdout), WASM transport (WasmMessageReader/Writer, push_lsp_message, start_snaq_lite_lsp). (2) **Phases:** Initialize/initialized; didOpen/didChange (full sync); diagnostics (parse/resolve errors → publishDiagnostics); hover (expression at offset, value + format); inlay hints (block children, format value). (3) **State:** Async lock (futures::lock::Mutex) around LspState (db, source, uri, version, root_def, root_spanned, unit_registry, diagnostics); set_eval_registry/set_stream_input_registry per request. (4) **Native:** run_native(stdin, stdout) with tokio; tower-lsp default. (5) **WASM:** tower-lsp runtime-agnostic, getrandom js; transport.rs with channel reader/writer, start_snaq_lite_lsp(postMessageCallback), push_lsp_message(s). (6) **Docs:** docs/LSP.md (capabilities, native stdio, WASM Worker API). (7) **Tests:** span_to_range (single line), run_error_to_diagnostic (parse kind). cargo test (workspace), clippy (snaq-lite-lsp) pass; wasm32 lib builds.
- **Stream import variance parameter:** (1) **Lang:** `stream_variance` module with `StreamVarianceMode` (Zero, InferFromDecimalPlaces) and `decimal_string_to_quantity(s)` (same rule as NumLiteral: no decimal → 0.5 abs_err, N decimals → 5×10^−(N+1), variance = err²). (2) **Ingest:** CsvParser with variance_mode (new/with_variance_mode); parser_for_format and parse_tabular take variance_mode; Parquet/Arrow ignore mode (always 0). (3) **CLI:** stream_feeder and stream_feed_dispatch take variance_mode; main parses `--stream-variance zero|infer` (default zero), passes to run_stream_mode and feed_stream_file_to_sender. (4) **Docs:** EXTERNAL_STREAMS.md — CLI option, infer from decimal places (text only), scientific use case. (5) **Tests:** stream_variance (decimal_string_to_quantity), csv_parser_infer_variance_from_decimal_places, feed_read_to_sender_infer_variance_from_decimal_places, cli_stream_variance_infer_succeeds. cargo test, clippy, wasm-pack test pass.
- **Review and improve (Parquet/Arrow test coverage):** (1) **Arrow file format:** Added arrow_parser_file_format_yields_records using FileWriter (IPC file with footer); exercises FileReader path in ArrowParser::parse; helper write_arrow_file() next to write_arrow_stream(). (2) **parse_tabular error path:** parse_tabular_arrow_invalid_bytes_returns_err when feature parquet (supported format, invalid content → Err). (3) **record_batch:** row_to_record_float32_becomes_numeric locks in Float32 → Numeric. (4) **parquet_parser round-trip:** Assert column names "a"/"b" in parquet_parser_round_trip_yields_records. Ingest with --features parquet: 32 tests; cargo test, clippy pass.
- **Test coverage (Parquet/Arrow/record_batch):** (1) **record_batch:** row_to_record_numeric_columns, row_to_record_null_becomes_undefined, row_to_record_unsupported_type_becomes_undefined, row_to_record_mixed_numeric_and_unsupported. (2) **parquet_parser:** parquet_parser_empty_input_returns_err, parquet_parser_invalid_bytes_returns_err, parquet_parser_round_trip_yields_records (ArrowWriter → ParquetParser). (3) **arrow_parser:** arrow_parser_float64_column, arrow_parser_null_becomes_undefined, arrow_parser_empty_batch_yields_no_rows; refactored with write_arrow_stream helper. (4) **format:** parse_tabular_arrow_success, parse_tabular_parquet_success (feature-gated). Ingest with `--features parquet`: 29 tests; workspace cargo test, clippy, wasm-pack test pass.
- **Parquet and Arrow streaming import:** (1) **TabularParser:** Trait now takes `Box<dyn ReadSeek>` (ReadSeek = Read + Seek + Send) so Parquet/Arrow file formats work. (2) **snaq-lite-ingest:** Optional `parquet` feature with arrow, parquet, arrow-ipc, bytes; record_batch.rs (Arrow RecordBatch row → Record); ParquetParser and ArrowParser (stream row-by-row; Parquet/Arrow buffer input then iterate batches→rows). (3) **Format:** detect_format adds ARROW1 magic; parser_for_format and is_supported return Parquet/Arrow when feature on. (4) **CLI:** Optional `parquet` feature (snaq-lite-ingest/parquet); default build stays light. (5) **Docs:** EXTERNAL_STREAMS.md — Parquet/Arrow supported with feature, streaming semantics, TabularParser. (6) **Tests:** Arrow IPC round-trip test (arrow_parser_stream_format_yields_records); format tests feature-gated. cargo test (workspace + ingest --features parquet), clippy, wasm-pack test pass.
- **Review and improve (stream import / tests):** (1) **format.rs:** Simplified parquet branch in detect_format (removed redundant magic check when extension is .parquet). (2) **Docs:** EXTERNAL_STREAMS.md — "Invalid line" clarified as numeric files; added "Invalid cell (CSV)" (non-numeric cell → stream error). (3) **stream_feed_dispatch tests:** Extracted feed_temp_file_and_collect(extension, contents, unique) to DRY temp-file feed-and-collect; unique suffix avoids parallel test file clash. (4) **csv_parser:** Test csv_parser_empty_file_returns_err (empty reader → Err). cargo test, clippy, wasm-pack test pass.
- **Test coverage (stream import features):** Added tests for (1) **snaq-lite-ingest format:** detect_format (.csv, .parquet, .arrow, .ipc, .txt/no ext, PAR1 magic), StreamFormat is_tabular/is_supported, parser_for_format (Csv only), parse_tabular success and unsupported format. (2) **CSV parser:** invalid number in cell → Err, headers-only → 0 rows, single data row, trimmed whitespace in cells. (3) **stream_feed_dispatch:** .txt file uses numeric feeder (scalars not maps), CSV headers-only → empty stream, CSV invalid number → error element in stream. cargo test, clippy, wasm-pack test pass.
- **CSV/Parquet/Arrow stream import (plan):** (1) **Lang:** map_registry now **shared** (Arc<Mutex<RegistryStore>) so feeder threads can register row-as-map; added Record type, record_to_value(entries), record_to_chunk_element(entries). (2) **snaq-lite-ingest:** New crate with TabularParser trait (parse(Box<dyn Read+Send>) → iterator of Result<Record, ParseError>), StreamFormat (Csv, Parquet, Arrow, NumericLines), detect_format(path, magic), parse_tabular(); CsvParser (headers + rows, cells → numeric/undefined). (3) **CLI:** stream_feed_dispatch::feed_stream_file_to_sender(path, sender) detects format by extension; .csv → tabular (parse_tabular, record_to_chunk_element, batch chunks); else → stream_feeder (newline-delimited numbers). (4) **Docs:** EXTERNAL_STREAMS.md — File formats (CSV tabular, Parquet/Arrow planned, numeric lines), example CSV $d.map(fn r => (r.x)); Summary note on pushChunkRows for WASM. (5) **Tests:** map_registry shared + record_to_value/record_to_chunk_element; ingest csv_parser (headers+rows, empty→undefined); stream_feed_dispatch feed_tabular_csv_to_sender_yields_map_elements; cli_stream_from_csv_prints_mapped_column ignored (subprocess hang in some envs). cargo test (379 passed, 24 ignored), cargo clippy --workspace, wasm-pack test pass.
- **Review and improve (CLI streaming):** (1) **DRY:** Extracted `print_numeric_quantity(result)` for CLI numeric output; used in run_standard and run_stream_mode (non-vector branch). (2) **Duplicate stream name:** Passing the same name twice (e.g. `--stream x=a.txt --stream x=b.txt`) now exits with "duplicate stream name: x". (3) **Feeder join:** After consuming the stream, join feeder threads and exit(1) with "stream feeder thread panicked" if any join returns Err. (4) **Docs:** EXTERNAL_STREAMS.md — CLI "Limits and edge cases" (duplicate name, file not found, invalid line, feeder panic). (5) **Tests:** Integration test uses unique temp file (process::id()); added cli_stream_duplicate_name_errors. cargo test, clippy, wasm-pack test pass.
- **CLI streaming from files:** (1) **CLI:** `--stream name=path` / `-s name=path` (repeatable); parse_args(); run_stream_mode() creates stream inputs, run_with_stream_inputs, spawns feeder threads (stream_feeder::feed_file_to_sender), block_on(stream.collect()), formats with format_value_for_display. (2) **stream_feeder.rs:** Newline-delimited numbers, batch 8192, invalid line → Err(InvalidArgument); feed_file_to_sender(path, sender), feed_read_to_sender(reader, sender). (3) **Lang:** format_value_for_display made pub for stream-element formatting. (4) **Docs:** EXTERNAL_STREAMS.md — CLI section (usage, file format, example). (5) **Tests:** stream_feeder unit tests (chunks+EOF, skip empty, invalid line); integration test cli_stream_from_file_prints_vector. cargo test (380 passed), cargo clippy --workspace, wasm-pack test pass.
- **Review and improve (WASM Host):** (1) **Validation:** stream_input_map values must be non-negative integers (reject fractional; clear error message). (2) **Constants:** CONSUMER_YIELD_EVERY (16), FEEDER_YIELD_EVERY (4) in host.rs. (3) **Docs:** EXTERNAL_STREAMS — streamInputMap non-negative integers; pushChunk fails when stream closed; startFeeder closes on read failure (consumer sees EOF); consumeOutputStream callbacks must not throw; indices not reclaimed (slots grow). ERRORS_AND_EDGE_CASES — new row "WASM Host API" (stream index not found, stream_input_map value, stream sender closed, run result consumed/not vector, chunk element, ReadableStream). (4) **Tests:** create_stream_input_returns_number, run_with_stream_inputs_scalar_returns_run_index_and_is_vector_false (wasm-pack test). cargo test, clippy, wasm-pack test pass.
- **WASM-Safe Host data parsing and async stream injection:** (1) **Lang:** `create_stream_input()` in stream_handle.rs returns (StreamHandleId, UnboundedSender<Chunk>); test create_stream_input_returns_id_and_sender. (2) **WASM deps:** wasm-bindgen-futures, web-sys (ReadableStream, ReadableStreamDefaultReader), js-sys, futures, salsa. (3) **WASM host (host.rs):** STREAM_HANDLES (index → id + sender), RUN_RESULTS (run index → Option<(Value, Database)>), create_stream_input_host, stream_input_map_from_js, run_with_stream_inputs_host, js_array_to_chunk, push_chunk_host, close_stream_host, take_run_result, yield_to_event_loop, value_to_js, consume_output_stream_host, parse_newline_delimited_numbers, start_feeder_host. (4) **JS API (lib.rs):** createStreamInput, runWithStreamInputs(script, streamInputMap) → { runIndex, isVector }, pushChunk(streamIndex, array), closeStream(streamIndex), startFeeder(streamIndex, readableStream), consumeOutputStream(runIndex, onChunk, onDone, onError). (5) **Event-loop yielding:** consumer yields every 16 elements; feeder yields every 4 reads. (6) **Docs:** EXTERNAL_STREAMS.md — "WASM Host (browser)" (I/O safeguards, Host API, Host execution loop). cargo test (376 passed), cargo clippy --workspace, wasm-pack test --node crates/snaq-lite-wasm pass.
- **Review and improve (external streams):** (1) **Docs:** ERRORS_AND_EDGE_CASES — row "Unbound stream input" and "Stream input not available" (consume-time); See also EXTERNAL_STREAMS. EXTERNAL_STREAMS — "Limits and platform" (run_numeric not applicable for stream-only; single-consumer handle; WASM: drive stream in async task). (2) **Consume-time error:** When `vector_into_stream(FromInput(id))` is called but the receiver was already taken or not registered, the stream now yields one `Err(StreamInputNotAvailable)` then completes instead of silently yielding nothing. RunErrorKind::StreamInputNotAvailable; OneErrorStream in queries; test vector_into_stream_from_input_when_handle_consumed_yields_one_error. (3) **API/docs:** StreamHandleId inner field made private (opaque); ChunkFlattenStream doc notes empty chunks are skipped. 375 tests, clippy, wasm-pack test pass.
- **External data as asynchronous streams:** Implemented plan: (1) **Stream handle:** `stream_handle.rs` — `StreamHandleId`, `Chunk` = `Vec<Result<Option<Value>, RunError>>`, thread-local registry, `register`/`take_receiver`, `ChunkFlattenStream` for element-wise yield. (2) **Salsa input:** `StreamInputRegistry` (name → handle id) in ir.rs; all run paths set it (empty map by default). (3) **Syntax/IR:** Lexer `DollarIdent(name)`, grammar Factor for `$name` → `ExternalStream(name)`; `ExprDef`/`ExprData`/`SpannedExprDefKind::ExternalStream`; resolve and CAS pass-through. (4) **Evaluation:** `value()` for `ExternalStream` reads registry, returns `Value::Vector(LazyVector::FromInput(handle_id))` or `UnboundStreamInput`; `LazyVector::FromInput(StreamHandleId)` in vector.rs. (5) **Streaming:** `vector_into_stream(FromInput(id))` takes receiver from registry, wraps in `ChunkFlattenStream`, yields elements; channel carries chunks, flattened to elements. (6) **Host API:** `run_with_stream_inputs(input, registry, stream_inputs)` returns `(Value, Database)`; docs/EXTERNAL_STREAMS.md and README table. (7) **Tests:** parse_dollar_ident, resolve_external_stream_passthrough, value with/without registry, $x*2 → Map over FromInput, vector_into_stream from registered receiver yields elements and EOF. Clippy fix: clone_on_copy in queries.rs (use *r.borrow()).
- **CLI --numeric: show result when not a number:** With `-n`/`--numeric`, the CLI now uses `run()` then tries `to_quantity`. If that fails (date, boolean, map, etc.), it still prints the result via `run_format` instead of exiting with an error. README updated: "With --numeric, if the result is not a single quantity, the CLI still prints the result in display form instead of erroring."
- **Review and improve (dates):** (1) **Docs:** DATES.md — added "Limits and edge cases" (run_numeric on date → error; vector of dates out of scope). ERRORS_AND_EDGE_CASES — new row "Invalid argument (date)" for run_numeric on date. COMPARISONS_AND_FUZZY_BOOL — "Date comparison" subsection (date-vs-date → FuzzyBool, overlap → uncertain) and See also link to DATES.md. LITERALS_AND_VALUES — run_numeric expects single numeric (date → error); display bullet mentions date. (2) **Test:** run_numeric_date_returns_err — run_numeric("@2026") and run_numeric("d = @2026-02-26; d") return Err(InvalidArgument with "date"). (3) **Code:** queries.rs Date+Numeric branch comment that secs as i64 truncation is intentional for second-scale arithmetic. All tests, clippy, wasm-pack test pass.
- **Granular dates (@ sigil):** (1) **Lexer:** Tok::TemporalLiteral(String), take_temporal_literal (ISO 8601 shapes), LexicalError::InvalidTemporal. (2) **IR/Grammar:** SpannedExprDefKind::LitTemporal/LitDate, ExprDef::LitTemporal/LitDate, ExprData::LitDate; Factor production for TemporalLiteral. (3) **date.rs:** Grain, GranularDate (anchor_epoch_sec, grain), min/max_epoch_sec, parse_temporal_literal, grain_for_time_unit_name. (4) **Resolve:** LitTemporal → LitDate via parse_temporal_literal; RunErrorKind::InvalidTemporalLiteral. (5) **Value/Scope/CAS:** Value::Date, StoredValue::Date; LitDate in build_expression, value(), expression_to_def, interner, convert, canonical, rewrite, substitute. (6) **Algebra:** add_values Date ± Time (Time dimension, grain check via at_least_as_fine_as); sub_values Date − Date → seconds, Date − Time via add(-Time); RunErrorKind::IncompatibleDateGrain. (7) **Comparison:** cmp_values (Date, Date) → bounded-interval overlap → FuzzyBool (true/false/uncertain(0.5)); Date vs non-Date → InvalidArgument. (8) **Grammar:** Single-comparison RHS changed from Expr to AddExpr so date-vs-date parses (e.g. @2025 < @2026). (9) **Docs:** docs/DATES.md; LITERALS_AND_VALUES, README, ERRORS_AND_EDGE_CASES updated. (10) **Tests:** parse_temporal_literal_factor, run_date_literal_valid_formats, run_date_binding_and_retrieval, run_date_invalid_temporal_literal_errors, run_date_plus_time_*, run_date_minus_*, run_date_compare_*. cargo test (362 passed, 24 ignored), cargo clippy --workspace, wasm-pack test --node crates/snaq-lite-wasm pass.
- **Review and improve (map work):** (1) **Docs:** ERRORS_AND_EDGE_CASES — run_numeric on map in "Operation not supported for vector"; Unknown property row notes maps support any key via `.key`, missing → undefined; row "Property/method on non-vector and non-map" clarifies index/member on scalar vs maps/vectors. VARIABLE_BINDINGS — maps added to allowed bindings. README — maps in intro, Maps bullet and table row linking to MAPS.md. MAPS.md — Limits and errors (run_numeric on map, index/member on scalar). LITERALS_AND_VALUES — Map in value types table; run_numeric expects single numeric (vector/map/undefined → error). (2) **Code:** scope.rs `from_value` doc updated (Err only for Symbolic; Vector and Map stored by id). (3) **Test:** run_map_duplicate_key_first_wins — `{ a: 1, a: 2 }.a` and `[a]` → first value (1). 350 tests, clippy, wasm-pack test pass.
- **Map type for snaq-lite:** (1) **Bracket key:** Lexer emits `BracketKey(trimmed_string)` for content between `[` and `]` when after postfix factor; grammar `PostfixFactor "[" BracketKey "]"` → Index(base, key_string). Vector index: key parsed as non-negative int or variable lookup. (2) **Map registry:** `map_registry.rs` (MapId, thread-local store, register/get/get_key); `Value::Map(u64)`, `StoredValue::Map(MapId)`; scope from_value/to_value. (3) **Map literal:** Grammar `{ Ident ":" Expr MapEntryListRest "}"` (at least one entry); empty `{}` remains block. IR: SpannedExprDefKind/ExprDef/ExprData MapLiteral; resolve and CAS (interner, convert, canonical, rewrite, substitute) handle MapLiteral. (4) **Eval:** build_expression and value() for MapLiteral; Member and Index on Value::Map use map_registry::get_key; missing key → Undefined. (5) **Tests:** parse_map_literal, parse_map_vs_block_disambiguation, run_map_literal, run_map_bracket_key_trimmed, run_map_missing_key_returns_undefined, run_map_binding, run_map_nested; run_bracket_key_trimmed, run_bracket_key_variable. **Docs:** docs/MAPS.md. Lexer: allow clippy::cognitive_complexity on next(). Full check: cargo test (341 passed), cargo clippy --workspace, wasm-pack test --node crates/snaq-lite-wasm pass.
- **Chained comparisons and |x| (plan):** (1) **|x|:** Lexer `Tok::Bar` for `|`; Factor `"|" Expr "|"` → `Call("abs", [e])`. (2) **Chained:** IR `SpannedExprDefKind::And`, `ExprDef::And`, `ExprData::And`; resolve, CAS (interner, convert, canonical, rewrite, substitute), queries (build_expression, value, expression_to_def). Grammar: 3-tier precedence (0: IfExpr + 8 inlined chained productions, 1: single comparison, 2: bare AddExpr); chained exactly three operands (a < b < c). Tests: parse_chained_*, parse_abs_pipe*, run_chained_*, run_abs_pipe. Docs: SYNTAX.md, FUNCTIONS.md, COMPARISONS_AND_FUZZY_BOOL.md. **Known:** Parser limitation (LALRPOP #596) in if/comparison RHS/vector list; ExprReset workaround causes shift/reduce conflicts. **Review and improve:** (1) SYNTAX.md — added "Known parser limitation" section. (2) ERRORS_AND_EDGE_CASES — Expected condition row extended for And/chained. (3) 22 affected tests marked `#[ignore = "parser limitation (LALRPOP #596): ..."]` so CI passes (332 passed, 24 ignored). (4) cargo test && cargo clippy --workspace pass.
- **Review and improve (STEM implementation):** (1) **Docs:** ERRORS_AND_EDGE_CASES — added median, quantile to Empty vector reduction and to Unknown method supported list; extended Invalid argument row (quantile p out of [0,1], corr non-numeric or < 2 pairs). FUNCTIONS.md — round() semantics (half away from zero); Statistics section clarified corr/median/quantile requirements (numeric, same dimension, empty → error). (2) **corr error:** pearson_correlation now returns InvalidArgument("corr: elements must be numeric (same dimension)") instead of UnsupportedVectorOperation when an element is not numeric. (3) **Tests:** run_stem_median_quantile_corr_edge_cases — [].median(), [].quantile(0.5) → EmptyVectorReduction; quantile(1.5)/quantile(-0.1) → InvalidArgument; corr length mismatch and corr([1],[2]) → need 2 pairs. 348 tests, clippy, wasm-pack test pass.
- **STEM math functions (plan):** Implemented built-ins and syntactic sugar per plan. (1) **Elementary:** abs, sign, floor, ceil, round, trunc, mod. (2) **Exp/log:** exp, ln, log, log10, log2. (3) **Inverse trig:** asin, acos, atan, atan2. (4) **Hyperbolic:** sinh, cosh, tanh, asinh, acosh, atanh. (5) **Powers:** pow, cbrt. (6) **Probability:** norm_cdf, norm_inv, factorial, binom, perm. (7) **Discrete:** gcd, lcm. (8) **Constant:** phi in symbol_registry. (9) **Statistics:** corr(X,Y), V.median(), V.quantile(p). (10) **Syntactic sugar:** ^ (pow), % (mod), n! (factorial), √ (sqrt), ∛ (cbrt). functions.rs: param_names, call_builtin, require_dimensionless; statrs for norm_cdf/norm_inv. queries.rs: corr special-case, median_from_collected, quantile_from_collected, pearson_correlation, collected_to_sorted_quantities; unary built-in list extended for .map(). Lexer: Caret, Percent, Bang, SqrtPrefix, CbrtPrefix. Grammar: PowBound (^), Term % PowBound, PostfixFactor "!", Factor "√"/"∛". Docs: FUNCTIONS.md (all new functions + sugar), VECTORS.md (median, quantile). Tests: run_stem_*, run_phi_constant, run_syntactic_sugar_*. 347 tests, clippy, wasm-pack test pass.

## Just completed (earlier)
- **Review and improve (dimension mismatch "none"):** (1) **DRY:** Added `Unit::dimension_label_for_error()` in unit.rs (scalar → "none", else format); `RunErrorKind::DimensionMismatch` and `QuantityError::DimensionMismatch`/`IncompatibleUnits` use it. (2) **IncompatibleUnits** now uses the same label so scalar is never shown blank. (3) **Tests:** unit_dimension_label_for_error (Unit::scalar() → "none", pascal → "pascal"); quantity_error_dimension_mismatch_scalar_shows_none (QuantityError::DimensionMismatch display). (4) **Docs:** systemPatterns — Errors bullet notes dimension_label_for_error(). 340 tests, clippy, wasm-pack test pass.
- **Review and improve (error location propagation):** (1) **Map branch:** When collecting map results, errors from apply_user_map_element (e.g. wrong arity, non-storable) now get the MethodCall span via `.map_err(|e| with_span_if_missing(e, expr.span(db)))`. (2) **Vector method helpers:** sum, mean, min, max, product, variance, stddev branches attach the method call's span to errors from sum_vector_elements, reduce_min_max, product_vector_elements, compute_variance_value via with_span_if_missing; mean() also passes expr.span(db) to div_values so division errors are located. (3) **Doc comments:** run_err_with_span and with_span_if_missing in queries.rs. (4) **Tests:** run_unknown_unit_as_includes_location_when_source_provided ("1 as foo"); run_if_expected_condition_includes_location_when_source_provided ("if 1 then 2 else 3"). 338 tests, clippy, wasm-pack test pass.
- **Dimension mismatch message (scalar vs unit):** When one side of a dimension mismatch is dimensionless, the CLI no longer shows a blank; it shows **none** (e.g. `dimension mismatch: none vs pascal` for `A = 31 pascal; A + 1`). `RunErrorKind::DimensionMismatch` and `QuantityError::DimensionMismatch` display left/right via a helper: scalar unit → `"none"`, else unit's normal display. Test `run_dimension_mismatch_scalar_vs_unit_shows_none`; docs ERRORS_AND_EDGE_CASES — Dimension mismatch row notes "none" for dimensionless.
- **Division by zero: ±∞ or undefined:** (1) **CAS rewrite:** nonzero/0 constant-folds to ±∞; 0/0 left as Div (no Quantity for undefined). (2) **Symbolic substitute:** nonzero/0 → ±∞ (av/bv in Rust); 0/0 → Err. (3) **value():** ExprData::Div when div_values returns DivisionByZero → Ok(Value::Undefined). run("0/0") → Undefined; run_numeric("0/0") → UndefinedResult. (4) **Tests:** run_division_by_zero_nonzero_yields_infinity expects ±∞; run_zero_over_zero_returns_err expects Value::Undefined and UndefinedResult; run_numeric_symbolic_div_by_zero_yields_infinity; run_arithmetic_with_infinity expects Ok(inf). Runtime error location tests use (1<2)/(2<1) (BooleanResult with span) instead of dimension mismatch (no span). (5) **Docs:** ERRORS_AND_EDGE_CASES — 0/0 yields undefined; systemPatterns — ±∞ and CAS bullets updated. 334 tests, clippy, wasm-pack test pass.
- **Review and improve (runtime error location):** (1) **Docs:** ERRORS_AND_EDGE_CASES.md — added "How runtime and parse errors are shown": when the CLI (or any formatter with source) reports an error it may show line/column and a snippet with squiggle; example for division by zero; if source is not available only the message is shown. (2) **Tests:** run_runtime_error_without_source_omits_location — format_run_error_with_source(&e, None) returns exactly "division by zero". run_runtime_error_multiline_includes_location_and_message — multi-line input "1 + 1\n2 / 0" still gets location and message (snippet may be first line or full program depending on CAS span). 334 tests, clippy, wasm-pack test pass.
- **Runtime error location and snippet:** Runtime errors (e.g. division by zero for `31 * 10/0`) now show source location and snippet when the CLI uses `format_run_error_with_source(&e, Some(&expression))`. (1) **CAS:** In `cas/rewrite.rs`, division-by-zero during constant folding now returns `RunError::at(span, RunErrorKind::DivisionByZero)` so the error has a span. (2) **Spans in evaluator:** `Expression` has optional `span: Option<Span>`; `ProgramDef` has `spanned_root: Option<SpannedExprDef>`. `run_with_registry` keeps the spanned root from `simplify_symbolic` and passes it to `ProgramDef::new(..., Some(root_spanned))`. `build_expression(db, def, spanned)` builds the Salsa graph from `SpannedExprDef` when present and sets each node's span. (3) **div_values** takes `span: Option<Span>` and uses `RunError::at(span, kind)` when returning DivisionByZero and other errors so runtime division errors get location. (4) **Test:** `run_runtime_error_shows_location_and_snippet` asserts that formatting a division-by-zero error with source includes "at line", "division by zero", and the source snippet. 332 tests, clippy, wasm-pack test pass.
- **CLI formatted errors for lexer (invalid float):** Lexer errors like "invalid float: $" had no span, so the CLI showed only the message. `LexicalError::InvalidFloat` now carries `{ snippet, start, end }`; lexer sets span in `take_num()` and in the catch-all `_` branch. Parser converts `ParseError::User(lex_err)` to `ParseError::with_span(...)` when `lex_err.span()` is `Some`. CLI now shows line/column and snippet with squiggle for e.g. `[1,2,-3].map(sqrt)$`. WASM test `evaluate_error_returns_parse_error_message` updated to accept "parse error at " as well as "parse error: ". 331 tests, clippy, wasm-pack test pass.
- **Review and improve (built-in functions in map):** (1) **Bug fix:** The `.map()` branch was using `default_si_registry()` instead of the registry from the enclosing `with_registry` closure; removed that line so `run_with_registry(custom)` is respected when applying the function to each element. (2) **Clippy:** Pass `registry` instead of `&registry` to `apply_user_map_element` and `apply_unary_builtin` (needless_borrow). (3) **Test:** `run_vector_map_sqrt_uses_eval_registry` — `run_with_registry("[1,4,9].map(sqrt)", &registry)` yields `[1,2,3]`, ensuring the eval registry is used. (4) **Docs:** ERRORS_AND_EDGE_CASES — Unknown method row now mentions `map(max)` (multi-arg built-in); VARIABLE_BINDINGS — cannot shadow list includes `sqrt` and `take`, and notes you can bind a built-in value to another name (e.g. `f = sqrt; [1,4,9].map(f)`); README — Vectors bullet mentions `.map(sqrt)`. 331 tests, clippy, wasm-pack test pass.
- **Built-in functions where a lambda is required:** `.map()` now accepts a **single-argument built-in** (e.g. `sqrt`, `sin`, `cos`, `tan`) as well as a user function. Bare identifiers that name built-ins resolve to `Value::BuiltinFunction(name)` (LitSymbol resolution: scope → built-in name → unit → symbolic). `StoredValue::BuiltinFunction(String)` allows `f = sqrt; [1,4,9].map(f)`. Map branch: if `Value::BuiltinFunction(name)` then require `param_names(name).len() == 1` else error; apply `apply_unary_builtin(name, &elem, registry)` per element. Multi-arg built-ins (e.g. `max`) in map return clear "one parameter" error. Display: built-in function values show as `<function>`. Exhaustive Value matches updated in lib.rs, queries.rs, symbolic.rs, scope.rs. Tests: run_vector_map_sqrt, run_vector_map_sin, run_vector_map_builtin_variable, run_vector_map_max_requires_one_parameter, run_builtin_function_displays_as_function. Docs: VECTORS.md and FUNCTIONS.md (Vector .map) state that .map accepts user function or single-arg built-in with example `[1,2,3].map(sqrt)`. 330 tests, clippy, wasm-pack test pass.
- **Review and improve (vector math methods):** (1) **mean():** Compute defined count `n` first; return `EmptyVectorReduction("mean")` when `n == 0` (avoids division by zero for all-sparse vectors). (2) **Variance/stddev:** Extracted `compute_variance_value(collected, registry)`; variance() and stddev() use it (stddev = sqrt of variance Value). (3) **sqrt negative:** Use `RunError::InvalidArgument(msg)` instead of `UnknownFunction` so error display is "invalid argument: sqrt: argument must be non-negative". (4) **Docs:** FUNCTIONS.md — sqrt error described as "Invalid argument"; ERRORS_AND_EDGE_CASES — new row "Invalid argument"; systemPatterns — InvalidArgument in Errors bullet. (5) **Tests:** run_sqrt_builtin expects `InvalidArgument` for sqrt(-1); run_vector_product_variance_stddev adds single-element case [7].variance() → 0, [7].stddev() → 0. 325 tests, clippy pass.
- **Vector mathematical methods (science/math students):** (1) **Built-in sqrt:** One-arg, non-negative; result unit to power 1/2; variance propagated; element-wise on vectors. param_names and call_builtin in functions.rs; apply_unary_builtin and eval_call vector branch include sqrt. (2) **Grammar:** ArgList allows empty (e.g. `V.sum()`). (3) **Vector methods** (MethodCall in queries.rs): **sum** (sum_vector_elements), **mean** (sum/length, empty → EmptyVectorReduction), **min**/**max** (reduce_min_max via call_builtin), **dot** (one vector arg, dot_product), **norm** (sum of squares then sqrt), **product** (product_vector_elements), **variance** (population: mean of squares − square of mean), **stddev** (sqrt of variance), **all**/**any** (FuzzyBool fold; empty → true/false). Helpers: product_vector_elements, reduce_min_max. (4) **FuzzyBool:** and_/or_ for combining (Uncertain×Uncertain → probability combination). (5) **RunError::EmptyVectorReduction(String).** (6) **Tests:** run_vector_sum/mean/min_max, run_sqrt_builtin/vector, run_vector_dot_norm, run_vector_product_variance_stddev, run_vector_all_any. (7) **Docs:** VECTORS.md (methods list, Math and statistics subsection, Limits updated); ERRORS_AND_EDGE_CASES (Unknown method, Empty vector reduction, sqrt in cannot shadow); FUNCTIONS.md (sqrt); README (Functions bullet, vector methods); systemPatterns (Member access methods, Functions sqrt, Errors EmptyVectorReduction). 325 tests, clippy, wasm-pack test pass.
- **Single-argument lambda shorthand:** Grammar allows `fn param => (body)` and `fn param => { block }` (anonymous) and `fn name param => (body)` / `fn name param => { block }` (named) so parentheses around the parameter list are optional for one parameter. expr.lalrpop: Factor and BlockItem each have two new productions. Same AST (Lambda with one (param, None)). Docs: FUNCTIONS.md and VECTORS.md mention shorthand. Tests: parse_single_arg_lambda_shorthand, run_single_arg_lambda_shorthand, run_vector_map_method now includes `[1,2,3].map(fn n => (n * 10))` → [10,20,30]. 317 tests, clippy, wasm-pack test pass.
- **Review and improve (vector methods/properties):** (1) **Docs:** ERRORS_AND_EDGE_CASES — added rows for Unknown property, Unknown method, Property/method on non-vector. VECTORS.md — noted .map is eager and that body can use closure. FUNCTIONS.md — new "Vector .map" subsection: function must have exactly one parameter. README — Vectors bullet now mentions .length, .map(fn), .take(start, length). (2) **Tests:** run_vector_map_requires_one_parameter (wrong-arity map returns first error); run_vector_map_with_closure ({ c = 10; [1,2,3].map(fn (x) => (x+c)) } → [11,12,13]). (3) **Behavior:** .map() now fails fast: first apply_user_map_element error (e.g. wrong arity) is returned as value() Err instead of a vector of errors. 315 tests, clippy, wasm-pack test pass.
- **Vector methods and properties (dot notation):** Property `V.length` (number of elements) and methods `V.map(fn (x) => (body))`, `V.take(start, length)`. Grammar: PostfixFactor "." MethodIdent "(" ArgList ")" → MethodCall, "." Ident → Member; lexer emits MethodIdent after "." when ident is followed by "(". IR: ExprDef/ExprData Member(base, name), MethodCall(base, name, args); CAS (interner, convert, canonical, rewrite, substitute) and resolve recurse. value(): Member → length via collect_vector_stream; MethodCall "take" → LazyVector::Take; "map" → eager (collect + apply_user_map_element per element, Salsa-safe). RunError: UnknownProperty, UnknownMethod. VectorMapOp::UserMap(id) and apply_map_op branch remain for potential future lazy map. Docs: VECTORS.md (Properties and methods), systemPatterns (Member/MethodCall, UserMap). Tests: parse_vector_property_length, parse_vector_method_map/take, run_vector_length, run_vector_map_method, run_vector_take_method, run_vector_unknown_property/method, run_vector_member_on_non_vector. 313 tests, clippy, wasm-pack test pass.
- **Vector variable binding:** `V = [1, 2, 3]` now succeeds. Added `vector_registry` (thread-local HashMap by VectorId), `StoredValue::Vector(VectorId)`; `from_value(Value::Vector(v))` registers and returns id; `to_value(StoredValue::Vector(id))` looks up and returns `Value::Vector`. Test renamed to `run_binding_vector_succeeds` (binding and using `V` both yield vector). Docs: VARIABLE_BINDINGS.md and ERRORS_AND_EDGE_CASES.md updated (vectors allowed; only symbolic binding not supported). queries.rs comment: param values storable, from_value rejects only Symbolic. 304 tests, clippy, wasm-pack test pass.
- **take(start, length) and vector indexing:** Built-in `take(vector, start, length)` returns a streaming vector (slice); 0-based, non-negative integers; special-cased in eval_call (param_names "vector","start","length"). `LazyVector::Take { source, start, length }`; `TakeVectorStream` in queries skips then yields; `vector_into_stream` handles Take. Index syntax: `V[index]` and `V.0`, `V.1` (PostfixFactor "[" Expr "]" or "." Num → `ExprDef::Index`). Lexer: `Dot` token when '.' follows a non-number token (`last_was_number` + `any_token_emitted` so ".5" at start and "1.2.3" stay numbers). IR/CAS: Index(base, index); value() → base vector, index integer (reject negative/non-integer → InvalidIndex), Take(_, index, 1), collect one element or IndexOutOfBounds. RunError: InvalidIndex, IndexOutOfBounds. Docs: VECTORS.md (Slicing and indexing), ERRORS_AND_EDGE_CASES (invalid index, index out of bounds, take in built-in shadow list). Tests: parse_vector_index_bracket/dot, parse_take_call, run_take_slice, run_vector_index_bracket/dot, run_vector_index_out_of_bounds, run_vector_index_invalid. 304 tests, clippy, wasm-pack test pass.
- **Review and improve (session):** (1) Test: `run_user_function_too_many_args` (sub(1,2,3) → "too many arguments (expected 2)"). (2) Docs: FUNCTIONS.md — added "Unknown parameter name" to user-defined errors; added note that when program result is a function it displays as `<function>`. ERRORS_AND_EDGE_CASES — Unknown function row now includes "unknown parameter name in a named argument". (3) CLI `-n` path: single evaluation — use `run()` once, then if `Value::Function` print `<function>`, else `to_quantity(&default_registry())` and format; avoids double evaluation when result is numeric. (4) Memory: this bullet. 296 tests, clippy, wasm-pack test pass.
- **Review and improve (user-defined functions):** systemPatterns: added bullet for user-defined functions (syntax fn/=>, Lambda/CallExpr, closure, scope-first call, CannotObfuscateBuiltin, Value::Function, StoredValue::Function, user_function registry, CAS). README: Functions bullet now mentions user-defined (fn, default params, block bodies, closures) and link to FUNCTIONS.md; Examples table added row for mysum(1,2)→3; API doc lists Function in Value. ERRORS_AND_EDGE_CASES: Unknown function row extended (missing arg, duplicate param, too many args, not callable); new row "Cannot shadow built-in function"; Binding value row mentions functions can be bound and function→quantity error; Binding limits list allowed (incl. user-defined function) and built-in names; See also FUNCTIONS.md. FUNCTIONS.md: new "Errors and edge cases (user-defined)" (required params, duplicate param, too many args, not callable). Tests: run_user_function_block_body (block body myCrazySumPlus42), run_user_function_missing_required_arg (needTwo(1) → missing 'b'). queries.rs: comment in eval_user_call that param values must be storable (from_value rejects Vector/Symbolic). 287 tests, clippy, wasm-pack test pass.
- **User-defined functions:** Anonymous `fn (params) => (expr)` or `=> { block }`; named `fn name(params) => ...` (FnName = Ident | FuncIdent so `fn mysum(a,b) => ...` parses). Params: `Ident` or `Ident = Expr` (defaults). eval_call: scope first (StoredValue::Function → eval_user_call), else built-in. eval_user_call: bind args (positional, named, defaults from closure), extend closure env, evaluate body. eval_binding_chain: reject binding if name is built-in (CannotObfuscateBuiltin). Value::Function(id), StoredValue::Function(id), UserFunction (params, body, closure_env); user_function.rs registry. CAS: canonical, rewrite, substitute handle Lambda/CallExpr. Display: Value::Function → "<function>". Docs: FUNCTIONS.md (user-defined syntax, defaults, block body, named/positional args, closures, no shadowing); VARIABLE_BINDINGS.md (cannot bind to sin/cos/tan/max/min, link to FUNCTIONS). Tests: parse_anonymous_lambda, parse_named_function, parse_lambda_default_param, run_named_function_call, run_user_function_default_param, run_user_function_named_args, run_user_function_closure, run_cannot_obfuscate_builtin_sin/max. 285 tests, clippy, wasm-pack test pass.
- **Review and improve (run_numeric variable shadowing):** Added tests: `run_numeric_nested_block_shadowing` (m=1; { m=2; m } → 2), `run_numeric_unbound_unit_name_still_errors` (DEF+2 without binding → Err). Clarified substitute.rs module doc: bound names left as LitSymbol so run_numeric resolves from scope (variables shadow units). 276 tests, clippy, wasm pass.
- **run_numeric variable shadowing:** `run_numeric("DEF=3; DEF+2")` now returns 5. `substitute_symbols` collects all names bound in the program (from Binding nodes) and leaves those as LitSymbol so `value()` resolves them from scope; other identifiers are substituted from symbol then unit registry. Test renamed to `run_numeric_variable_shadowing_unit_uses_scope` (expects Ok(5)). VARIABLE_BINDINGS.md and systemPatterns.md updated: variable bindings that shadow unit names work in both run() and run_numeric().
- **Review and improve (variables shadow units):** systemPatterns: Variable bindings bullet now describes scope-first LitSymbol resolution and that bare identifiers resolve to LitSymbol; Value and symbols bullet notes run_numeric uses substitute_symbols(symbol_registry, unit_registry) with no scope; CAS bullet documents substitute_symbols(symbol_registry, unit_registry). VARIABLE_BINDINGS.md Limits: added note that run_numeric has no block scope when substituting, so variable bindings that shadow unit names only affect run(), not run_numeric(). Test run_numeric_variable_shadowing_unit_uses_unit_not_scope: run_numeric("DEF=3; DEF+2") must be Err (SymbolHasNoValue or DimensionMismatch), not 5. queries.rs: comment in LitSymbol branch "Scope first (variables shadow units), then unit, then symbolic." 274 tests, clippy, wasm pass.
- **Variables shadow units (DEF=3; DEF+2):** "DEF" was parsed as unit decafarad (prefix "da" + "F"), so it never got scope lookup. Fixed with scope-first identifier semantics: (1) **Resolve:** bare identifiers stay as `LitSymbol` (no unit lookup at resolve time). (2) **Eval (value()):** for `LitSymbol(name)` use scope first, then unit registry (1 in that unit), then symbolic. (3) **run_numeric:** `substitute_symbols` now takes unit_registry and substitutes unit names as `Lit(Quantity::new(1.0, unit))` so `run_numeric("1 m")` etc. still work. Test `run_variable_shadows_unit_def` (DEF=3; DEF+2 => 5). `resolve_180_times_degrees_as_unit` updated to assert eval result instead of resolved tree. 273 tests, clippy, wasm pass.
- **Chained assignment (A = B = 42):** Grammar: RHS of `=` is `BlockItem` so assignment is right-associative; `A = B = 42` parses as `Binding(A, Binding(B, 42))`. Evaluation: added `eval_binding_chain(db, scope, expr)` returning `(Value, Scope)` so that each binding in the chain extends scope; Block and standalone Binding use it so all names get bound and the expression value is the innermost RHS. Tests: `parse_chained_assignment` (A = B = 42 → Binding(A, Binding(B, Lit))), `run_chained_assignment` (x = y = 42 → 42; x = y = 42 then x + y → 84). Doc: VARIABLE_BINDINGS.md mentions chained assignment and example.
- **Assignment as expression (value = RHS):** A binding is an expression whose value is the RHS. Block evaluation now returns the last binding’s value when the last item is a binding (instead of Undefined). queries.rs: in Block loop, when processing a Binding and `i == n - 1`, return `Ok(v)`. Test renamed: `run_block_only_bindings_returns_last_rhs` (`x = 1\ny = 2` → 2). Docs: VARIABLE_BINDINGS.md and BLOCKS_AND_EXPRESSIONS.md updated (result = last expression or last binding’s RHS).
- **Review and improve (variable bindings):** Dedicated `RunError::BindingValueNotSupported(String)` for binding symbolic or vector values (replacing misuse of `SymbolHasNoValue`/`UnsupportedVectorOperation`). `StoredValue::from_value` returns it for Symbolic/Vector. Tests: `run_binding_through_full_pipeline` (parse → resolve → simplify_symbolic → program → value gives 11 for `x = 10\nx + 1`), `run_binding_symbolic_returns_binding_value_not_supported`, `run_binding_vector_returns_binding_value_not_supported`. Doc: `docs/VARIABLE_BINDINGS.md` Limits section now states runtime returns an error when binding symbolic/vector. 270 tests, clippy, wasm-pack test pass.
- **Review and improve (multi-expression and blocks):** README: Value list includes Undefined; API bullets describe multiple expressions (newline/`;`) and blocks, result = last value or Undefined; run_numeric/run_scalar document UndefinedResult for empty input or empty block. lib.rs: run() doc mentions multi-expr and blocks; run_numeric/run_scalar docs mention UndefinedResult. New test run_numeric_and_run_scalar_empty_or_undefined_return_undefined_result (run_numeric(""), run_scalar(""), run_numeric("{}"), run_scalar("{}") → Err(UndefinedResult)). docs/BLOCKS_AND_EXPRESSIONS.md: Undefined section clarifies run_numeric("") and run_numeric("{}") examples. 262 tests, clippy, wasm-pack test pass.
- **Multi-expression and blocks:** Top-level and inside blocks: multiple expressions per program/block (newline- or `;`-separated; blank lines allowed). Program/block result = last expression value; empty block or empty program → `Value::Undefined`. Lexer: `LBrace`, `RBrace`, `Semicolon`, `Newline` (only space/tab skipped). Grammar: `Program` = `ExprList` => `Block(list)`; `ExprList`/`ExprListRest` (separator-only allowed for blank lines); `Factor` has `{ ExprList }`. IR: `ExprDef::Block(Vec<ExprDef>)`, `ExprData::Block(Vec<Expression>)`; `Value::Undefined`; `RunError::UndefinedResult`. Resolve, queries (build_expression, value, expression_to_def), CAS (interner, convert, canonical, rewrite, substitute) handle Block. Tests: run_multiple_expressions_newline/semicolon_last_wins, run_block_returns_last_expression, run_empty_block_returns_undefined, run_block_as_expression, run_nested_blocks, run_blank_lines_allowed, run_empty_returns_undefined, parse_empty_returns_empty_block. Doc: `docs/BLOCKS_AND_EXPRESSIONS.md`. 261 tests, clippy, wasm-pack test pass.
- **WASM tests fix:** `wasm-pack test --node crates/snaq-lite-wasm` was failing because `getrandom` (transitive via statrs → rand → rand_core) does not support `wasm32-unknown-unknown` by default. Added `[target.'cfg(target_arch = "wasm32")'.dependencies]` in `crates/snaq-lite-wasm/Cargo.toml` with `getrandom = { version = "0.2", features = ["js"] }` so the crate is built with the "js" feature for WASM. All 4 WASM tests pass; cargo test and clippy pass.
- **Review and improve (numeric precision / tilde):** New error `RunError::TildeRequiresNumeric` when either side of `~` is not numeric (symbolic, boolean, or vector); `PrecisionMustBePositive` reserved for RHS numeric but ≤ 0 or non-finite. README and `run()` doc now describe implicit precision from decimal places and `value ~ error`. `NumLiteral` exported from crate. Tests: `run_tilde_requires_numeric_operands` (e.g. `pi ~ 2`), `run_literal_decimal_places_affect_variance` (`10.5` vs `10.50` variances). 253 tests, clippy pass.
- **Implicit and explicit numeric precision:** **Phase 1 (implicit):** Lexer emits `NumLiteral { raw, value }`; `NumLiteral` in ir.rs with `decimal_places_after()` (mantissa only), `implicit_absolute_error()` (no decimal → 0.5, N decimals → 5×10^−(N+1)), `implicit_variance()`. Resolve builds `Quantity::with_number(SnaqNumber::new(value, implicit_variance()), unit)`. Grammar/IR use `LitScalar(NumLiteral)`, `LitWithUnit(NumLiteral, String)`. Tests: num_literal_implicit_variance_from_decimal_places, run_literal_implicit_variance_preserved. Comparison tests updated to use decimal literals (e.g. 1.0 < 2.0) for crisp results. **Phase 2 (tilde):** Lexer `Tok::Tilde`; grammar `TildeBound` (PostfixFactor ~ TildeBound | PostfixFactor), Term uses TildeBound; `ExprDef::WithPrecision(l, r)`, `ExprData::WithPrecision`. value(): RHS central value only (variance discarded), variance = error²; RHS ≤ 0 or non-finite → `RunError::PrecisionMustBePositive`. CAS: ExprNode::WithPrecision; convert, canonical, rewrite, substitute pass through. Tests: parse_tilde_precedence, run_tilde_explicit_variance, run_tilde_negative_error_returns_err, run_tilde_zero_error_returns_err, run_tilde_rhs_variance_discarded. 251 tests, clippy pass.
- **If/then/else with fuzzy superposition:** Syntax `if condition then expression else expression`. Lexer keywords if, then, else; grammar IfExpr; ExprDef::If, ExprData::If; resolve and CAS (interner, convert, canonical, rewrite, substitute) handle If. value(): condition must be FuzzyBool; True/False → single branch; Uncertain(p) → superposition: numeric blend after converting both branches to common unit (smaller_unit), then mean/variance via Law of Total Variance; symbolic weighted sum (P·then + (1−P)·else). RunError::ExpectedCondition, IfBranchTypeMismatch. **Review and improve:** numeric blend fixed to convert qa/qb to result_unit before blending; tests run_if_superposition_numeric_different_units, run_if_superposition_dimension_mismatch; docs/memory note unit conversion. 244 tests, clippy pass.
- **Review and improve (Statistical comparisons / FuzzyBool):** Centralized comparison logic in `stat_compare`: added `ComparisonKind` (Eq, Ne, Lt, Le, Gt, Ge), `MEANS_EQUAL_TOLERANCE` (1e-15), and `comparison_probability(kind, na, nb)` so queries and CAS rewrite share one implementation. Replaced duplicated match/closures in `queries.rs` and `cas/rewrite.rs` with calls to `comparison_probability` and `cmp_lit_fuzzy_with_kind`. Guarded `probability_a_gt_b` against non-finite result (return 0.5). Added tests: `comparison_probability_ge_equal_means`, `run_format_fuzzy_bool_uncertain`. All 235 tests and clippy pass.
- **Statistical comparisons and FuzzyBool:** Comparisons return **Value::FuzzyBool** (True / False / Uncertain(prob)). Added `FuzzyBool` (fuzzy.rs), `stat_compare` (probability_a_gt_b, probability_to_fuzzy_bool, CONFIDENCE_THRESHOLD 0.95), dependency `statrs` for erf. IR/CAS: `LitFuzzyBool(FuzzyBool)` replaces LitBool; rewrite folds comparisons using P(A>B) and threshold. Runtime `cmp_values` uses statistical comparison; when var_diff==0 or means equal (Ge/Le) crisp. Eq/Ne: crisp when zero variance; with variance use 2*min(p,1-p) and complement. Display: "true"/"false"/"uncertain(prob)". All 233 tests and clippy pass.
- **Review and improve (boolean comparison):** README: Value types include Bool; comparison result described as boolean; examples use true/false; run_numeric documents BooleanResult. Doc comments: ir (ExprDef comparison → Value::Bool), symbolic (SymbolicExpr comparison → boolean), cas/interner (LitBool), vector (bool per element). New test `run_format_vector_of_booleans` ([1 < 2, 1 == 2] → "[true, false]"). Memory bullets trimmed for consistency. All 228 tests and clippy pass.
- **Review and improve (comparison operators):** README and docs updated for boolean results (true/false). vector.rs: comparison semantics (element-wise, outer, row×column). queries.rs: `cmp_values` returns Value::Bool. Tests: run_comparison_format ("true"/"false"), run_comparison_symbolic_display, run_comparison_row_column_reduce, run_format_vector_of_booleans. All 228 tests and clippy pass.
- **Comparison binary operators (==, !=, <, <=, >, >=):** Same dimension required; result is Value::Bool. Precedence: comparisons bind weaker than +, -, as. IR/CAS: Eq, Ne, Lt, Le, Gt, Ge; rewrite folds to LitBool. cmp_values: scalar → Bool or Symbolic comparison; vector×scalar → Map; vector×vector → ZipMap/Outer/row×column. Tests: parse_comparison, run_comparison_*, run_format_vector_of_booleans.
- **Review and improve (CAS vector×vector):** Comment in canonical.rs clarified: order preserved only for **binary** vector×vector (exactly two operands); 3+ operands still sorted. Added test `canonicalize_preserves_column_times_row_order` (column×row outer stays column then row). All 217 tests and clippy pass.
- **CAS: preserve vector×vector Mul order:** Canonicalization no longer reorders multiplication when both operands are vector-valued (`VecLiteral` or `Transpose`), so row×column (dot) and column×row (outer) stay as written. `canonical.rs`: `rank_is_vector_valued`; Mul branch skips sort when `sorted.len() == 2` and both ranks are vector-valued. Tests `canonicalize_preserves_vector_times_vector_order`, `canonicalize_preserves_column_times_row_order`; dot-product tests expect scalar; `[]' * []` → `"0"`. All 217 tests and clippy pass.
- **Review and improve (vector–vector):** Fixed `LazyVector::Outer` doc: yields one column per **right** (row) element, column j = [op(left_i, right_j)]. Module doc in vector.rs: short summary of vector–vector orientation semantics (ZipMap, Outer, dot). Empty vectors: outer product with empty left column → zero columns (stream goes to Done when `left_buffer.is_empty()`); test `run_vector_vector_empty` for `[]' * []` and `[] * [1,2]'` → `"[]"`. All 215 tests and clippy pass.
- **Vector–vector operations by orientation:** Binary ops (add, sub, mul, div) between two vectors dispatch on orientation: column×column and row×row → element-wise (`LazyVector::ZipMap`), result column or row; column×row → outer product (`LazyVector::Outer`), result vector of column vectors (matrix); row×column → dot product (mul) or sum op sum (add/sub), scalar; row×column div → error. Added `VectorBinaryOp`, `VectorLengthMismatch`, `ZippedVectorStream`, `OuterProductVectorStream`; `add_values`/`sub_values`/`mul_values`/`div_values` take `db: Option<&dyn Database>` for vector×vector; dot/sum use `collect_vector_stream` + `dot_product`/`sum_vector_elements`. Tests: run_vector_vector_element_wise, run_vector_vector_outer_product, run_vector_vector_dot_product, run_vector_arithmetic_errors (length mismatch). CAS preserves vector×vector Mul order (see above). All 216 tests and clippy pass.
- **Review and improve (vector orientation):** Added test `run_vector_orientation_column_row_transpose`: literal → Column, single transpose → Row, double transpose → Column, Map preserves orientation (`[1,2,3]' + 0` → Row). Doc: vector module states display is `"<vector>"` for all (Option A) and that transpose flips / Map preserves orientation; `VectorValue` doc mentions literal and 2D column yields are column, transpose flips to row, Map preserves. All 211 tests and clippy pass.
- **Vector orientation (column / row):** Every vector has orientation: **Column** (default) or **Row**. `Value::Vector` now holds `VectorValue { inner: LazyVector, orientation }`. Literals → column; transpose (`'`) flips orientation (wraps inner in `LazyVector::Transpose`); Map preserves operand orientation; 2D transpose column yields → column. Streaming uses `v.inner`; display remains `"<vector>"`. Added `VectorOrientation`, `VectorValue` (column/row/transpose); re-exported from crate. All 210 tests and clippy pass.
- **Review and improve (recursive vector CLI rendering):** Doc comments: `format_value_for_display` now states that sparse elements are shown as `?`; `run_with_registry_format` states that nested vectors are fully expanded with an example. Inline comment in the Vector branch clarifying that sparse (undefined) elements display as `"?"`. Tests: `run_format_scalar` (2+3 → "5", 1 m + 2 m → "3 m") and `run_format_symbolic` (1+pi → "1 + π") to guard the unified format path for non-vector results. All 210 tests and clippy pass.
- **Vector transpose postfix (`'`):** Postfix `'`; chained transpose; 2D streaming via `TransposedVectorStream`. **Review and improve:** Empty-rows 2D case fixed ([[],[],[]]' → []): always switch to YieldingColumns when all elements are vectors, so max_cols=0 yields zero columns. Store max_cols in YieldingColumns to avoid recompute. Doc for TransposedVectorStream; test for [[],[],[]]'.
- **Review and improve (implicit vector mapping):** `vector_into_stream` now matches on `vector` by value (no clone): `FromEvaluated` then `Map` then `FromExprs`/stubs → Empty. Doc comments: when Map elements are computed (on consume); `apply_map_op`/`apply_unary_builtin` and LazyVector::Map reference `vector_into_stream`. Tests: `run_vector_scalar_binary_mapping_with_units` ([1 m, 2 m] + 3 m → [4 m, 5 m]); `run_vector_unary_func_mapping` extended with sin([0 rad]) → [0]. 203 tests, clippy pass.
- **Implicit vector mapping (broadcasting):** Binary ops (add, sub, mul, div) and unary (neg, sin, cos, tan) accept vector + scalar or vector-only; result is a lazy `LazyVector::Map { source, op: VectorMapOp }`. `VectorMapOp`: Add/SubRhs/SubLhs/Mul/DivRhs/DivLhs(Box<Value>), Neg, UnaryFunc(String). Streaming: `vector_into_stream` recurses on Map and wraps in `VectorStream::Mapped`; `apply_map_op` applies op per element using `with_registry` and existing value helpers; `apply_unary_builtin` for sin/cos/tan on one Value. value(): add/sub/mul/div return Map for vector–scalar; neg_value returns Map for vector; eval_call returns Map for unary built-ins (sin, cos, tan) when single arg is vector. Vector–vector remains UnsupportedVectorOperation. Tests: run_vector_scalar_binary_mapping, run_vector_unary_neg_mapping, run_vector_unary_func_mapping, run_vector_chained_mapping, run_vector_arithmetic_errors (vector+vector). 202 tests, clippy pass.
- **CLI vector printing:** `run_format()` / `run_with_registry_format()` in lang crate return a display string; for `Value::Vector` they collect the stream and format as `[e1, e2, ...]`. CLI (non-numeric path) now uses `run_format()` so vectors print as `[1, 2, 3]`, `[1 m, 2 m]`, `[]`, etc.
- **Vector type and literal:** Lazy vector type: `Value::Vector(LazyVector)`, `LazyVector` (FromExprs, FromEvaluated, Transform/Transpose stubs). Stream API: `vector_into_stream(db, lv)` returns `VectorStream` (Unpin); elements pre-evaluated in value() for literals (Salsa constraint). Vector literal syntax `[ expr, ... ]` or `[]`: lexer LBracket/RBracket, grammar VecExprList, `ExprDef::VecLiteral`, `ExprData::VecLiteral`; resolve and CAS (interner, convert, substitute, canonical, rewrite) recurse. RunError::UnsupportedVectorOperation for arithmetic/to_quantity on vectors. Display `<vector>`. Tests: parse_vector_literal, run_vector_literal_returns_vector, run_numeric_vector_returns_err, run_vector_arithmetic_errors, run_vector_stream_yields_elements. WASM build verified. No Tokio in deps (futures only). 198 tests, clippy pass.

- **Review and improve (time units):** Time block in unit_registry now uses named constants `HOURS_PER_DAY`, `DAYS_PER_WEEK`, `DAYS_PER_YEAR_JULIAN` for day/week/year registration; comment notes Julian year for calendar-style durations. Added `si_same_dimension_time_units` test (all time units pairwise same dimension). `time_units_convert_correctly` now asserts plural "days" and "weeks" resolve via singularize; uses `SECONDS_PER_DAY` and relative tolerance for factor asserts. SI_DERIVED_UNITS comment documents time-factor derivation. All 193 tests and clippy pass.
- **Missing time units:** Added day (24 h), week (7 d), year (365.25 d, Julian), month (year/12), quarter (year/4), decade, century, millennium to `default_si_registry()` in unit_registry.rs. Doc comment updated; SI_DERIVED_UNITS extended; `time_units_convert_correctly` and `run_numeric_time_units_convert_and_add`; unsupported list updated. All 192 tests and clippy pass.

## Just completed (earlier)
- **Area dimension and area units:** Area base dimension; m2, are (100 m²), hectare (100 are = 10 000 m², hect-are via prefix "hect"); prefix "hect" in prefix.rs for hectare. Squared-length lookup: prefix + "m2" or "m²" → (prefix·m)² so km2, cm2, mm2 convert correctly without explicit registration. Units: m2, m², sqm, squaremeter, square_meter; squarekilometer, square_kilometer; squarecentimeter, square_millimeter; are, hectare, ha; inch (in), in2, in², sqin, squareinch, square_inch; **foot (ft), ft2, sqft, squarefoot, square_foot**. Unsupported list: removed "inch", "foot". **Review and improve:** Module doc mentions squared-length resolution; INCH_TO_M/FOOT_TO_M used for inch/foot and in2/ft2; added area_ha_alias_equals_10000_m2 test; extended area_parsing to include sqft, squarefoot, ft2, foot, ft. All 189 tests and clippy pass.
- **"as" keyword for unit conversion:** Lexer: `As` token (boundary check so "aspect" stays Ident). Grammar: `UnitTerm` (IdentIdents, *, /, per) and `Expr "as" UnitTerm`. IR: `ExprDef::As(Box<ExprDef>, Box<ExprDef>)`, `ExprData::As`. Resolve: `resolve_unit_expr` for RHS (LitUnit/Mul/Div only → Lit(Quantity), UnknownUnit if unknown ident). CAS: `ExprNode::As(l, r)` in interner, convert, canonical, rewrite, substitute (pass through, no fold). Queries: build_expression and value() for As; numeric left → convert_to → Value::Numeric; symbolic left → scale_expr_to_unit → Value::Symbolic. IncompatibleUnits mapped to DimensionMismatch. Tests: parse_as_unit_conversion, run_as_simple_and_compound_unit, run_as_dimension_mismatch_errors, run_as_precedence. README and systemPatterns updated.
- **Review and improve (implicit mul / NumIdents / IdentIdents):** README: added "2 pi rad", "pi rad", "sin(pi rad)" to Language and Examples. Grammar: **PiIdents** + **IdentsRest** so "π rad" and "π rad degree" parse like "pi rad" (unicode π supported). Test: `parse_unicode_pi_rad_implicit_mul` (run_numeric("π rad"), sin(π rad) ≈ 0). systemPatterns and activeContext updated. All 175 tests and clippy pass.
- **Implicit mul for "2 pi rad":** "2 pi rad" and "1 s" parse as one factor via **NumIdents** (number + one or more idents → left-associated Mul chain). Grammar: added `NumIdents` (Num Ident, NumIdents Ident); Factor includes NumIdents so " / 1 s" divides by 1 s; removed `Num Ident` → `LitWithUnit`. Resolve attributes idents to units or symbols. Tests: `parse_quantity_literal`, `parse_ident_containing_per_still_ident` expect Mul chain; `parse_2_pi_rad_implicit_mul`; `parse_implicit_mul_rhs_not_ident` (e.g. "2 3 m" still parse error). systemPatterns updated.
- **Review and improve (singularization):** Refactored `get_unit_with_prefix`: extracted `try_lookup(ident, prefixes_by_len)` to avoid duplicated exact+prefix logic; doc states returned Unit always uses canonical (singular) key. README: built-in units sentence now mentions meter, second and that plural names (meters, seconds, kilometers) are accepted and normalized. Test: `run_per_alias_for_division` asserts compound unit with plural uses canonical singular (e.g. "kilometers" → "kilometer" in factors). All 172 tests and clippy pass.
- **Singularization for units:** Plural unit names (e.g. "meters", "seconds") now resolve by normalizing to singular before lookup. Added `singularize` crate; `UnitRegistry::get_unit_with_prefix` tries exact and prefix match, then singularize(ident) and retry. Removed all explicit plural registrations from `default_si_registry` (amperes, meters, seconds, grams, volts, etc.); registry stores singular forms only. Tests: `second_and_seconds_recognized_as_time_units` expect "second"; `run_long_form_units_kilometers` expects "kilometer"; new `plural_unit_input_resolves_to_canonical_singular`. systemPatterns and unit_registry doc updated. All 172 tests and clippy pass.
- **sin(180 * degrees) = 0 fix:** `sin(180 * degrees)` was returning ≈ -0.8 because `UnitRegistry::to_base_unit_representation` did not incorporate the defining unit's factor when resolving derived aliases (e.g. "degrees" → degree → π/180 rad). So 180 degrees converted to rad as 180 instead of π. Fix: in the Derived branch, multiply `result_factor` by `def_base_factor` so the chain degrees → degree → rad yields π/180. Tests: `resolve_180_times_degrees_as_unit`, `simplify_sin_180_times_degrees_arg_is_180_degree`, `run_sin_180_times_degrees_equals_zero`. All 169 tests and clippy pass.
- **Symbolic trig at well-known angles:** Named constants `sqrt_2`, `sqrt_3` in symbol registry (display √2, √3; numeric value for substitution). Trig sin/cos/tan at well-known angles (0, π/6, π/4, π/3, π/2, π and degree equivalents) return symbolic exact results (0, 1, √2/2, √3/2, 1/2, √3, 1/√3, -1). Numeric path (`run_numeric`, `to_quantity`) substitutes constants so result is a number; symbolic path keeps constants in display. `KnownAngle`, `known_angle_from_rad`, `known_angle_from_symbolic`, `exact_sin/cos/tan` in functions.rs; eval_call in queries uses them. Tests: run_sin_pi_fourth_symbolic_sqrt2_over_2, run_numeric_sin_pi_fourth_is_number, run_numeric_trig_result_is_number, run_symbolic_trig_well_known_angles; run_sin_pi_returns_numeric_zero updated to allow symbolic 0. All 163 tests and clippy pass.
- **Review and improve (angle/trig):** ExpectedAngle now shows "(dimensionless)" when argument is scalar (was empty); hint simplified to "e.g. sin(pi * rad)". Trig logic extracted to `eval_trig` helper (sin/cos/tan share one path). Doc: try_eval_symbolic_call example sin(pi * rad); unit_registry comment "SI base units (7) + rad (Angle)". Test: run_sin_90_degree_equals_one. All 157 tests and clippy pass.
- **Angle dimension and trig:** Angle dimension with rad (base) and degree (π/180 rad), degrees alias. Trig (sin, cos, tan) require argument of Angle dimension; `require_angle` converts to rad, then f64 sin/cos/tan. Wrong dimension → `RunError::ExpectedAngle { actual }`; if actual is dimensionless, message suggests adding rad (e.g. sin(pi * rad)). Tests: angle_rad_degree_same_dimension_and_convert, run_sin_180_degree_equals_zero, run_function_dimension_mismatch_errors (sin(1 m) and sin(pi) → ExpectedAngle; hint for dimensionless). README and systemPatterns updated. All 156 tests and clippy pass.
- **Trig variance propagation:** sin, cos, tan in `functions.rs` now propagate argument variance via first-order propagation Var(f(x)) ≈ (f'(x))² Var(x). Result built with `Quantity::with_number(SnaqNumber::new(value, variance), unit)`. Tan sets result variance to 0 when result is non-finite (e.g. near pole). Tests: `trig_sin_propagates_variance`, `trig_cos_propagates_variance`, `trig_tan_propagates_variance`, `trig_tan_near_pole_succeeds_and_variance_non_negative`. systemPatterns.md updated. All 154 tests and clippy pass.
- **Miniature CAS:** Four-phase CAS in `cas/`: (1) Interning (`ExprId`, `ExprNode`, `ExprInterner`) with ExprDef ↔ interned conversion; (2) Canonicalization (rank, flatten Add/Mul, sort by rank); (3) Term rewriting (identity/annihilation, constant fold with UnitRegistry, like-term collection; only scalar Lits as coefficients in monomials; division by zero in rewrite → RunError::DivisionByZero); (4) Evaluation: `simplify_symbolic(def, registry)` and `simplify_numeric(def, unit_registry, symbol_registry)`; `substitute_symbols(def, symbol_registry)`. `run_with_registry` uses CAS symbolic pipeline before program/value; `run_numeric_with_registry` uses substitute then CAS numeric pipeline. Tests: interning round-trip/dedup, canonicalize flatten/sort, rewrite identity/annihilation/constant fold/like terms, run_cas_symbolic_like_terms_2pi_plus_3pi, run_cas_numeric_2pi_plus_3pi. Division-by-zero in constant folding returns DivisionByZero (run_division_by_zero_nonzero_yields_infinity and run_arithmetic_with_infinity updated). All 134 tests and clippy pass.
- **Review and improve (symbols):** README updated for `run` → `Value`, `run_numeric`, `run_scalar`, symbols, and CLI `--numeric`. Symbolic substitution: division-by-zero during substitute now returns `RunError::DivisionByZero` (not `SymbolHasNoValue`). Doc: `SymbolRegistry` (symbols without value = not in map), `Value` (Display and `to_quantity`). Test: `run_numeric_symbolic_div_by_zero_returns_division_by_zero` for `(1+pi)/0`. All 118 tests and clippy pass.
- **Symbols:** Symbols (e.g. pi, π, e or any unknown identifier) are supported. Default evaluation returns `Value` (symbolic by default): `1 + π` → "1 + π", `3 + 2 + π + 1` → "6 + π". `SymbolRegistry` (built-ins pi/π, e), `SymbolicExpr`/`SymbolicQuantity` with simplification (Sum/Product), `Value::Numeric`|`Value::Symbolic`. IR: `LitSymbol`; resolve treats unknown idents as symbols; grammar has `"π"` → LitSymbol("pi"). `run()` returns `Result<Value, RunError>`; `run_numeric()` substitutes and returns `Quantity`; CLI `--numeric` for numeric output; WASM `evaluate_numeric()`.

## Just completed (earlier)
- **Unary minus:** Grammar and IR now support a leading minus (e.g. `-1`, `-(2*3)`). Added `ExprDef::Neg` and `ExprData::Neg`; Factor rule `"-" Factor => Neg`; resolve and value() handle Neg via `Quantity::neg`. Tests: `parse_unary_minus`, `eval_unary_minus`. All tests and clippy pass.

## Just completed (earlier)
- **Review and improve (SnaqNumber):** `SnaqNumber` now derives `Copy` (removes unnecessary clones in Quantity). Variance clamped to non-negative in Mul, Div, Mul<f64>, Div<f64> for float safety. Doc comments: propagation rules on `SnaqNumber`, `Quantity::value`/`variance` documented. Test `snaq_number_variance_always_non_negative` added. All 98 tests and clippy pass.
- **SnaqNumber migration:** Numeric values are now `SnaqNumber` (value + variance). Variance is propagated through Add/Sub/Mul/Div/Neg and scaling by f64; literal variance derived from f64 precision `(value * ε/2)²`. `Quantity` uses `number: SnaqNumber`; `Quantity::new(f64, unit)` / `from_scalar(f64)` build via `SnaqNumber::from_literal`; arithmetic uses `Quantity::with_number`. `SnaqNumber` re-exported from crate; `Quantity::value()` and `Quantity::variance()` accessors. All tests and clippy pass.

## Just completed (earlier)
- **Unit display fix:** Division units (e.g. `kilometers / hour`) now display as `kilometers/hour` instead of `kilometers/hour⁻¹`. In `Unit::fmt`, denominator factors use positive exponent for display (slash already means "per"). Test `unit_display_division_no_redundant_inverse` added. All tests and clippy pass.

## Just completed (earlier)
- **Phase 2 – Syntax and evaluation:** Grammar supports quantity literals (`Num`, `Num Ident`, `Ident`); parser produces `LitScalar`/`LitWithUnit`/`LitUnit`; two-step resolve converts to `Lit(Quantity)`. IR uses `ExprDef::Lit(Quantity)`; `value()` returns `Result<Quantity, RunError>`. `run()` returns `Result<Quantity, RunError>`; `run_scalar()` for dimensionless result. Errors: UnknownUnit, DimensionMismatch, DivisionByZero. CLI/WASM use `run()` and Display. All tests and clippy pass.
- **Phase 1 – Core model and SI:** Dimension, Unit, Quantity, prefix, unit/dimension registries; default_si_registry (m, s, kg, km, hour, minute).

## Just completed (Phase 3)
- **Phase 3 – Prefixes and simplification:** Prefix symbols in `prefix.rs`; `get_unit_with_prefix` in registry (longest-match prefix + base unit); Unit Display with prefix (e.g. km, µm) and ·/solidus/⁻¹; `Quantity::full_simplify` (dimensionless → scalar); `Quantity::full_simplify_with_registry` (prefer value in [0.1, 1000]).

## Just completed (Phase 4)
- **Phase 4 – Unit/dimension parity:** Extended default registry with Numbat-style units: Length (mile, au, parsec, light_year), Energy (joule, eV). Same names and conversion factors as Numbat for expressions like `1 mile`, `1 parsec`, `1 eV`, `100 km / hour`. More modules (time, imperial, cgs, etc.) can be added to the same built-in registry.

## Review and improve (done)
- **README:** Updated API to `run()` → `Quantity`, `run_scalar()`; added unit examples (100 km/hour, 1 mile, 1.5 km + 500 m).
- **Tests:** Added `run_division_by_zero_returns_err` (1/0, 3 m / 0 s) and `run_dimension_mismatch_returns_err` (1 m + 1 s).
- **API:** `run_with_registry(input, &UnitRegistry)` for custom registries; `run()` delegates to it. Exported `UnitRegistry`; added `Clone` to `UnitRegistry` and `DimensionRegistry`. Doc comments clarified (default units, when to use `run_scalar`).

## Just completed
- **SI units:** All BIPM SI base units (m, kg, s, A, K, mol, cd) and SI derived units with special names: J, C, V, F, ohm, S, Wb, T, H, Hz, N, Pa, W, lm, lx, Bq, Gy, Sv, kat; long forms (coulomb, volt, farad, ohm, siemens, weber, tesla, henry, lumen, lux, becquerel, gray, sievert, katal) and celsius. Removed these from unsupported list; tests and clippy pass.
- **Review and improve:** README updated with full SI unit list and examples (1 second, 1 volt). Unit registry refactored with `add_derived_alias` helper to reduce repetition; long-form base unit aliases added (ampere, amperes, kelvin, kelvins, mole, moles, candela, candelas, gram, grams). Test `long_form_base_unit_aliases` added. All tests and clippy pass.

## Just completed
- **Division alias "per":** Grammar accepts `per` as alias for `/` (e.g. `3 kilometers per hour`). LALRPOP string literal has higher lexer priority than Ident, so "per" is parsed as operator. Test `run_per_alias_for_division`; README and systemPatterns updated.
- **Review and improve (per):** Grammar comment on lexer priority for "per"; README example shows result (`3 km/hour`); `run()` doc mentions "per"; added `parse_per_same_as_div`, `parse_ident_containing_per_still_ident`, and `run_scalar("6 per 2")` in `eval_div`. All tests and clippy pass.

## Just completed
- **Implicit multiplication:** Grammar uses `ImplicitMulRight` (Num or `"(" Expr ")"`) so only number or parenthesized expr can be RHS of implicit mul (e.g. `10 20` → 200, `2 (3 + 4)` → 14, `10 m 2` → 20 m). `10 m` stays one quantity literal; no Term-Ident implicit mul. Tests: `parse_implicit_mul`, `eval_implicit_mul`, `eval_implicit_mul_with_units`. Test `parse_invalid_float_is_error` renamed to `parse_1_2_3_as_implicit_mul` (e.g. `1.2.3` now parses as 1.2 * .3 = 0.36). systemPatterns updated.

## Review and improve (implicit multiplication)
- **Docs:** systemPatterns.md now states implicit mul only when RHS is number or (expr); README Language + Examples mention implicit mul with `10 20`, `2 (3+4)`; `run()` doc in lib.rs updated.
- **Tests:** `parse_quantity_literal` asserts "10 m" is LitWithUnit (not Mul); `eval_implicit_mul` adds "10 20 + 5" == 205 (precedence); `parse_implicit_mul_rhs_not_ident` documents that "2 3 m" is parse error (RHS of implicit mul cannot be Ident). All tests and clippy pass.

## Just completed
- **Review and improve (division by zero / ±∞):** Added `Quantity::is_infinite()` and `Quantity::is_finite()`; `convert_to` short-circuits when value is infinite (same dimension, unit change only). `Quantity::div` uses `is_sign_positive()` for ±∞. `QuantityError::DivisionByZero` documented as 0/0 (indeterminate). Test `quantity_convert_to_preserves_infinity`; div tests assert `is_infinite()`/`is_finite()`. README: division-by-zero note and example (`1/0` → `inf`). All tests and clippy pass.
- **Division by zero and ±infinity (no NaN):** Nonzero/0 → ±∞ (sign of numerator); 0/0 → DivisionByZero. SnaqNumber::from_literal sets variance 0 for infinite x. Quantity::div branches: 0/0 Err, nonzero/0 Ok(±∞). Tests: run_division_by_zero_nonzero_yields_infinity, run_zero_over_zero_returns_err, run_arithmetic_with_infinity; quantity and SnaqNumber tests for inf and div-by-zero.

## Just completed
- **Review and improve (function calling):** README documents function calls and built-ins (sin, cos, tan, max, min), with examples `sin(pi)`, `max(3, 2)`, `max(a: 1, b: 2)`. Suppressed generated-parser unused_imports warning via `#[allow(unused_imports)]` on lalrpop_mod!(expr). Tests: `run_sin_pi_returns_numeric_zero` (run("sin(pi)") → Numeric 0), `run_function_wrong_arity_errors` (sin(1,2), max(1)), `run_function_dimension_mismatch_errors` (sin(1 m), max(1 m, 2 s)). Doc for `try_eval_symbolic_call` in functions.rs. All 148 tests and clippy pass (no warnings).
- **Function calling:** Custom lexer emits `FuncIdent` when identifier is followed by `(`, so `sin(1)` parses as call. Grammar: `FuncIdent "(" ArgList ")"`, `CallArg` = named (`Ident ":" Expr`) or positional (`Expr`). IR: `ExprDef::Call(name, Vec<CallArg>)`, `ExprData::Call(name, Vec<(Option<String>, Expression)>)`. Resolve and CAS (interner, convert, canonical, rewrite) handle Call; substitute recurses into call args. `functions.rs`: built-ins sin, cos, tan (one dimensionless arg), max, min (two same-dimension args); param names for binding. Queries: `eval_call` binds positional then named args, evaluates; symbolic result when any arg symbolic. `SymbolicExpr::Call` for display. Errors: `UnknownFunction`. Tests: parse sin/max/named, eval sin(0), max/min, run_numeric sin(pi), run_symbolic sin(x), unknown function.

## Just completed
- **Immutable variable bindings:** Lexical scoping with persistent env (`im::OrdMap`), Salsa-interned `Scope`, `value(db, scope, expr)`. Syntax: `Ident = Expr` in block/top-level (single `=`). StoredValue: Numeric, FuzzyBool, Undefined (no Vector/Symbolic in v1). Resolve and CAS handle `Binding`; Block evaluates bindings then last expression; LitSymbol → scope lookup then symbolic. Tests: parse_binding, run_binding_simple, run_binding_block, run_binding_shadowing, run_block_only_bindings_returns_undefined. Doc: docs/VARIABLE_BINDINGS.md.

## Next steps
- Optional: add more unit modules (imperial: foot, pound; etc.) or .nbt loader for full Numbat module set.
- Optional: variance in symbolic form; more built-in constants (φ, c). Variable binding to symbolic (vector binding is implemented).
