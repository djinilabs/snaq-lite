# Optional: base URL for a future REST/API backend (e.g. auth, persistence).
# This frontend is a SPA; the backend is independent of TanStack Start.
# Leave empty to use no backend.
VITE_API_BASE_URL=
